--[[
â•”â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•—
â•‘      AvenL Hub V20 - BROOKHAVEN VERSION             â•‘
â•‘                                                      â•‘
â•‘  ðŸ‘‘ DONO: AVENTUREIROTURBO                          â•‘
â•‘  â­ VICE-DONO: MARIO                                â•‘
â•šâ•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")

print("AvenHub V21 - Carregando...")
local player = Players.LocalPlayer
if not player.Character then player.CharacterAdded:Wait() end

local screenParent
pcall(function() screenParent = game:GetService("CoreGui") end)
if not screenParent then screenParent = player:FindFirstChildOfClass("PlayerGui") end

-- Limpar instancias antigas
for _, v in pairs(screenParent:GetChildren()) do
    if v.Name == "AvenHub_V20_FINAL" or v.Name == "AvenHub_V21_FINAL" then
        pcall(function() v:Destroy() end)
    end
end

-- Fallback setclipboard seguro para todos executors
local _rawClip = setclipboard
local function _safeClip(text)
    if _rawClip then pcall(_rawClip, tostring(text))
    elseif toclipboard then pcall(toclipboard, tostring(text)) end
    print("Copiado: " .. tostring(text):sub(1,30))
end
setclipboard = function(t) _safeClip(t) end

-- Mensagens iniciais Brookhaven
task.spawn(function()
    local ok, remote = pcall(function()
        return ReplicatedStorage:WaitForChild("RE", 5):WaitForChild("1RPNam1eTex1t", 5)
    end)
    if ok and remote then
        local msgs = {"{â—„executed}", "AvenL Hub V20", "Brookhaven+ Edition"}
        for _, msg in ipairs(msgs) do
            pcall(function() remote:FireServer("RolePlayBio", msg) end)
            task.wait(0.7)
        end
    end
end)

-- Cores
local C = {
    BG = Color3.fromRGB(10,10,18),
    PANEL = Color3.fromRGB(16,16,26),
    CARD = Color3.fromRGB(24,24,38),
    BORDER = Color3.fromRGB(50,50,75),
    ACCENT1 = Color3.fromRGB(120,70,255),
    ACCENT2 = Color3.fromRGB(70,190,255),
    GREEN = Color3.fromRGB(60,220,120),
    RED = Color3.fromRGB(220,60,80),
    ORANGE = Color3.fromRGB(255,150,40),
    YELLOW = Color3.fromRGB(255,220,60),
    GOLD = Color3.fromRGB(255,215,0),
    TEXT = Color3.fromRGB(230,230,255),
    DIM = Color3.fromRGB(140,140,185),
    WHITE = Color3.new(1,1,1),
}

-- Helpers
local function corner(p,r) local c=Instance.new("UICorner",p); c.CornerRadius=UDim.new(0,r or 8); return c end
local function stroke(p,col,t) local s=Instance.new("UIStroke",p); s.Color=col or C.BORDER; s.Thickness=t or 1.5; return s end
local function grad(p,c0,c1,rot) local g=Instance.new("UIGradient",p); g.Color=ColorSequence.new(c0,c1); g.Rotation=rot or 0; return g end
local function tween(obj,props,t,style,dir)
    pcall(function()
        TweenService:Create(obj,TweenInfo.new(t or 0.2,style or Enum.EasingStyle.Quad,dir or Enum.EasingDirection.Out),props):Play()
    end)
end

local function lbl(parent,text,size,pos,fs,col,xalign)
    local l=Instance.new("TextLabel",parent)
    l.Size=size; l.Position=pos or UDim2.new(0,0,0,0); l.Text=text; l.TextSize=fs or 12
    l.TextColor3=col or C.TEXT; l.BackgroundTransparency=1; l.Font=Enum.Font.GothamBold
    l.TextXAlignment=xalign or Enum.TextXAlignment.Center
    l.TextYAlignment=Enum.TextYAlignment.Center
    return l
end

local function btn(parent,text,size,pos,bg,fs)
    local b=Instance.new("TextButton",parent)
    b.Size=size; b.Position=pos or UDim2.new(0,0,0,0); b.Text=text; b.TextSize=fs or 12
    b.Font=Enum.Font.GothamBold; b.TextColor3=C.WHITE; b.BackgroundColor3=bg or C.CARD
    b.AutoButtonColor=false; corner(b,8)
    b.MouseEnter:Connect(function() tween(b,{BackgroundTransparency=0.25},0.12) end)
    b.MouseLeave:Connect(function() tween(b,{BackgroundTransparency=0},0.12) end)
    return b
end

local function actionBtn(parent,text,color,lo,cb)
    local b=btn(parent,text,UDim2.new(1,0,0,32),nil,color)
    b.LayoutOrder=lo or 99
    if cb then b.MouseButton1Click:Connect(cb) end
    return b
end

local function toggleBtn(parent,offTxt,onTxt,offCol,onCol,lo,cb)
    local state=false
    local b=actionBtn(parent,offTxt,offCol,lo)
    b.MouseButton1Click:Connect(function()
        state=not state
        b.Text=state and onTxt or offTxt
        tween(b,{BackgroundColor3=state and onCol or offCol},0.15)
        if cb then cb(state) end
    end)
    return b,function() return state end
end

local function textbox(parent,placeholder,lo)
    local t=Instance.new("TextBox",parent)
    t.Size=UDim2.new(1,0,0,36); t.BackgroundColor3=C.BG; t.TextColor3=C.TEXT
    t.TextSize=12; t.Font=Enum.Font.Gotham; t.PlaceholderText=placeholder or ""
    t.PlaceholderColor3=C.DIM; t.ClearTextOnFocus=false; t.LayoutOrder=lo or 99
    t.TextWrapped=false; t.TextXAlignment=Enum.TextXAlignment.Left
    local pad=Instance.new("UIPadding",t)
    pad.PaddingLeft=UDim.new(0,10); pad.PaddingRight=UDim.new(0,10)
    corner(t,7); stroke(t,C.ACCENT1,1); return t
end

local function section(page,title)
    local card=Instance.new("Frame",page)
    card.Size=UDim2.new(1,-6,0,0); card.AutomaticSize=Enum.AutomaticSize.Y
    card.BackgroundColor3=C.CARD; card.LayoutOrder=#page:GetChildren()
    corner(card,10); stroke(card,C.BORDER,1)
    local inner=Instance.new("Frame",card)
    inner.Size=UDim2.new(1,-14,0,0); inner.Position=UDim2.new(0,7,0,0)
    inner.AutomaticSize=Enum.AutomaticSize.Y; inner.BackgroundTransparency=1
    local il=Instance.new("UIListLayout",inner)
    il.Padding=UDim.new(0,6); il.SortOrder=Enum.SortOrder.LayoutOrder
    local ip=Instance.new("UIPadding",inner)
    ip.PaddingTop=UDim.new(0,8); ip.PaddingBottom=UDim.new(0,8)
    if title then
        local tl=lbl(inner,"â–¸ "..title,UDim2.new(1,0,0,20),nil,12,C.ACCENT2,Enum.TextXAlignment.Left)
        tl.LayoutOrder=0
        local sep=Instance.new("Frame",inner)
        sep.Size=UDim2.new(1,0,0,1); sep.BackgroundColor3=C.BORDER
        sep.BorderSizePixel=0; sep.LayoutOrder=1
    end
    return inner
end

local function notify(title, msg, duration)
    duration = duration or 3
    local notif = Instance.new("Frame")
    notif.Size = UDim2.new(0, 300, 0, 70)
    notif.Position = UDim2.new(1, -320, 0, 20)
    notif.BackgroundColor3 = C.PANEL
    notif.BorderSizePixel = 0
    notif.ZIndex = 1000
    corner(notif, 10); stroke(notif, C.ACCENT1, 2)
    lbl(notif, title, UDim2.new(1, -20, 0, 25), UDim2.new(0, 10, 0, 5), 13, C.ACCENT2, Enum.TextXAlignment.Left)
    local msgLbl = lbl(notif, msg, UDim2.new(1, -20, 0, 35), UDim2.new(0, 10, 0, 30), 11, C.TEXT, Enum.TextXAlignment.Left)
    msgLbl.TextWrapped = true
    notif.Parent = screenParent
    tween(notif, {Position = UDim2.new(1, -320, 0, 20)}, 0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
    task.delay(duration, function()
        tween(notif, {Position = UDim2.new(1, 0, 0, 20)}, 0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In)
        task.wait(0.35); notif:Destroy()
    end)
end

-- Sistema de notificaÃ§Ã£o anti-troll
local trollNotifQueue = {}
local function notifyTroll(playerName, action)
    local notif = Instance.new("Frame")
    notif.Size = UDim2.new(0, 280, 0, 75)
    notif.Position = UDim2.new(1, -300, 1, 0)
    notif.BackgroundColor3 = Color3.fromRGB(40, 10, 10)
    notif.BorderSizePixel = 0
    notif.ZIndex = 2000
    corner(notif, 12)
    stroke(notif, Color3.fromRGB(255, 50, 50), 3)
    local glow = Instance.new("UIGradient", notif)
    glow.Color = ColorSequence.new(Color3.fromRGB(80, 20, 20), Color3.fromRGB(40, 10, 10))
    glow.Rotation = 90
    local alertIcon = lbl(notif, "âš ï¸", UDim2.new(0, 40, 0, 40), UDim2.new(0, 8, 0, 8), 28, Color3.fromRGB(255, 80, 80))
    local titleLbl = lbl(notif, "ALERTA DE TROLL", UDim2.new(1, -60, 0, 20), UDim2.new(0, 52, 0, 6), 11, Color3.fromRGB(255, 100, 100), Enum.TextXAlignment.Left)
    titleLbl.Font = Enum.Font.GothamBold
    local nameLbl = lbl(notif, playerName, UDim2.new(1, -60, 0, 18), UDim2.new(0, 52, 0, 26), 13, Color3.fromRGB(255, 200, 200), Enum.TextXAlignment.Left)
    nameLbl.Font = Enum.Font.GothamBold
    local actionLbl = lbl(notif, action, UDim2.new(1, -60, 0, 24), UDim2.new(0, 52, 0, 46), 10, Color3.fromRGB(255, 150, 150), Enum.TextXAlignment.Left)
    actionLbl.TextWrapped = true
    notif.Parent = screenParent
    local offset = #trollNotifQueue * 85
    table.insert(trollNotifQueue, notif)
    local targetPos = UDim2.new(1, -300, 1, -(85 + offset))
    tween(notif, {Position = targetPos}, 0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
    task.spawn(function()
        for i = 1, 3 do
            task.wait(0.3)
            tween(stroke(notif, Color3.fromRGB(255, 100, 100), 3), {Thickness = 5}, 0.15)
            task.wait(0.15)
            tween(notif:FindFirstChildOfClass("UIStroke"), {Thickness = 3}, 0.15)
        end
    end)
    task.delay(15, function()
        tween(notif, {Position = UDim2.new(1, -300, 1, 0)}, 0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In)
        task.wait(0.35)
        for i, n in ipairs(trollNotifQueue) do
            if n == notif then table.remove(trollNotifQueue, i); break end
        end
        for i, n in ipairs(trollNotifQueue) do
            local newPos = UDim2.new(1, -300, 1, -(85 + (i-1) * 85))
            tween(n, {Position = newPos}, 0.3)
        end
        notif:Destroy()
    end)
end

print("AvenHub V21 - Criando GUI...")
local ok, err = pcall(function()

-- GUI
local gui=Instance.new("ScreenGui")
gui.Name="AvenHub_V21_FINAL"
gui.ResetOnSpawn=false
gui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
gui.IgnoreGuiInset=true
gui.Parent=screenParent

-- Splash
local splash=Instance.new("Frame",gui)
splash.Size=UDim2.new(1,0,1,0); splash.BackgroundColor3=C.BG; splash.ZIndex=100
local icon=lbl(splash,"âš¡",UDim2.new(0,100,0,100),UDim2.new(0.5,-50,0.5,-80),70,C.ACCENT2)
icon.ZIndex=102; icon.TextTransparency=1
local splashTitle=lbl(splash,"AvenL Hub V21 FINAL",UDim2.new(0,500,0,40),UDim2.new(0.5,-250,0.5,30),24,C.WHITE)
splashTitle.ZIndex=102; splashTitle.TextTransparency=1
task.spawn(function()
    tween(icon,{TextTransparency=0},0.4); task.wait(0.3)
    tween(splashTitle,{TextTransparency=0},0.4); task.wait(1)
    tween(splash,{BackgroundTransparency=1},0.4)
    tween(icon,{TextTransparency=1},0.4)
    tween(splashTitle,{TextTransparency=1},0.4)
    task.wait(0.5); splash:Destroy()
end)

-- BotÃ£o Flutuante
local floatBtn=Instance.new("TextButton",gui)
floatBtn.Size=UDim2.new(0,65,0,65)
floatBtn.Position=UDim2.new(0,16,0.65,0)
floatBtn.Text=""; floatBtn.BackgroundColor3=C.BG
floatBtn.Active=true; floatBtn.Draggable=true; floatBtn.ZIndex=10
corner(floatBtn,33); stroke(floatBtn,C.ACCENT1,2)
lbl(floatBtn,"âš¡",UDim2.new(1,0,0,40),UDim2.new(0,0,0,2),20,C.ACCENT2)
lbl(floatBtn,"AVEN",UDim2.new(1,0,0,16),UDim2.new(0,0,1,-18),8,C.DIM)

-- BotÃ£o de Pular
local jumpBtn=Instance.new("TextButton",gui)
jumpBtn.Size=UDim2.new(0,70,0,70)
jumpBtn.Position=UDim2.new(0,16,0.5,0)
jumpBtn.Text=""; jumpBtn.BackgroundColor3=C.PANEL
jumpBtn.Active=true; jumpBtn.Draggable=true; jumpBtn.ZIndex=10
corner(jumpBtn,35); stroke(jumpBtn,C.GREEN,2)
lbl(jumpBtn,"â¬†ï¸",UDim2.new(1,0,0,40),UDim2.new(0,0,0,2),24,C.GREEN)
lbl(jumpBtn,"PULAR",UDim2.new(1,0,0,16),UDim2.new(0,0,1,-18),8,C.DIM)
jumpBtn.MouseButton1Click:Connect(function()
    local char=player.Character
    if char then
        local hum=char:FindFirstChildOfClass("Humanoid")
        if hum and hum:GetState()~=Enum.HumanoidStateType.Freefall then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
            tween(jumpBtn,{Size=UDim2.new(0,75,0,75)},0.1)
            tween(jumpBtn,{Size=UDim2.new(0,70,0,70)},0.2)
        end
    end
end)

-- Janela Principal
local WW,WH=560,450
local main=Instance.new("Frame",gui)
main.Size=UDim2.new(0,WW,0,WH)
main.Position=UDim2.new(0.5,-WW/2,0.5,-WH/2)
main.BackgroundColor3=C.BG
main.Active=true; main.Draggable=true
corner(main,14)

local rgbStroke = Instance.new("UIStroke", main)
rgbStroke.Thickness = 2.5
rgbStroke.Color = Color3.fromRGB(255, 0, 0)
task.spawn(function()
    local hue = 0
    while main and main.Parent do
        hue = (hue + 1) % 360
        rgbStroke.Color = Color3.fromHSV(hue / 360, 1, 1)
        task.wait(0.05)
    end
end)

-- TÃ­tulo
local tbar=Instance.new("Frame",main)
tbar.Size=UDim2.new(1,0,0,44); tbar.BackgroundColor3=C.PANEL
tbar.ZIndex=2; corner(tbar,14)
local tfix=Instance.new("Frame",tbar)
tfix.Size=UDim2.new(1,0,0.5,0); tfix.Position=UDim2.new(0,0,0.5,0)
tfix.BackgroundColor3=C.PANEL; tfix.BorderSizePixel=0; tfix.ZIndex=2
lbl(tbar,"âš¡",UDim2.new(0,26,1,0),UDim2.new(0,9,0,0),18,C.ACCENT2).ZIndex=3
lbl(tbar,"AvenL Hub V20 | BROOKHAVEN+",UDim2.new(1,-80,1,0),UDim2.new(0,38,0,0),13,C.TEXT,Enum.TextXAlignment.Left).ZIndex=3
local closeBtn=btn(tbar,"âœ•",UDim2.new(0,26,0,26),UDim2.new(1,-34,0.5,-13),C.RED,13)
closeBtn.ZIndex=3
closeBtn.MouseButton1Click:Connect(function()
    tween(main,{Size=UDim2.new(0,WW,0,0)},0.2); task.wait(0.22)
    main.Visible=false; main.Size=UDim2.new(0,WW,0,WH)
end)

-- Sidebar
local sidebar=Instance.new("Frame",main)
sidebar.Size=UDim2.new(0,108,1,-44)
sidebar.Position=UDim2.new(0,0,0,44)
sidebar.BackgroundColor3=C.PANEL
sidebar.BorderSizePixel=0; sidebar.ZIndex=1
local sbLayout=Instance.new("UIListLayout",sidebar)
sbLayout.Padding=UDim.new(0,4)
sbLayout.HorizontalAlignment=Enum.HorizontalAlignment.Center
Instance.new("UIPadding",sidebar).PaddingTop=UDim.new(0,8)

-- Content
local content=Instance.new("Frame",main)
content.Size=UDim2.new(1,-114,1,-50)
content.Position=UDim2.new(0,112,0,48)
content.BackgroundTransparency=1; content.ZIndex=1

local isOpen=true
floatBtn.MouseButton1Click:Connect(function()
    if isOpen then
        tween(main,{Size=UDim2.new(0,WW,0,0)},0.2); task.wait(0.22)
        main.Visible=false; isOpen=false; main.Size=UDim2.new(0,WW,0,WH)
    else
        main.Visible=true; main.Size=UDim2.new(0,WW,0,0)
        tween(main,{Size=UDim2.new(0,WW,0,WH)},0.25,Enum.EasingStyle.Back,Enum.EasingDirection.Out)
        isOpen=true
    end
end)

-- Sistema de Abas
local tabs={}; local activeTab=nil

local function newTab(name,icon)
    local tb=Instance.new("TextButton",sidebar)
    tb.Size=UDim2.new(0,94,0,68); tb.Text=""
    tb.BackgroundColor3=C.CARD; tb.AutoButtonColor=false
    tb.ZIndex=2; corner(tb,9)
    local ico=lbl(tb,icon,UDim2.new(1,0,0,38),UDim2.new(0,0,0,5),20,C.DIM); ico.ZIndex=3
    local txt=lbl(tb,name,UDim2.new(1,0,0,20),UDim2.new(0,0,1,-24),9,C.DIM); txt.ZIndex=3
    local ind=Instance.new("Frame",tb)
    ind.Size=UDim2.new(0,3,0.55,0); ind.Position=UDim2.new(0,0,0.225,0)
    ind.BackgroundColor3=C.ACCENT1; ind.Visible=false; ind.ZIndex=3; corner(ind,3)
    local page=Instance.new("ScrollingFrame",content)
    page.Size=UDim2.new(1,0,1,0); page.BackgroundTransparency=1
    page.ScrollBarThickness=3; page.ScrollBarImageColor3=C.ACCENT1
    page.CanvasSize=UDim2.new(0,0,0,0)
    page.AutomaticCanvasSize=Enum.AutomaticSize.Y
    page.Visible=false; page.ZIndex=1; page.BorderSizePixel=0
    local pl=Instance.new("UIListLayout",page)
    pl.Padding=UDim.new(0,7); pl.SortOrder=Enum.SortOrder.LayoutOrder
    local pp=Instance.new("UIPadding",page)
    pp.PaddingTop=UDim.new(0,5); pp.PaddingRight=UDim.new(0,5)
    local tab={btn=tb,page=page,icon=ico,text=txt,ind=ind}
    table.insert(tabs,tab)
    tb.MouseButton1Click:Connect(function()
        if activeTab==tab then return end
        if activeTab then
            activeTab.page.Visible=false
            tween(activeTab.btn,{BackgroundColor3=C.CARD},0.15)
            tween(activeTab.icon,{TextColor3=C.DIM},0.15)
            tween(activeTab.text,{TextColor3=C.DIM},0.15)
            activeTab.ind.Visible=false
        end
        activeTab=tab; page.Visible=true
        tween(tb,{BackgroundColor3=C.BORDER},0.15)
        tween(ico,{TextColor3=C.ACCENT2},0.15)
        tween(txt,{TextColor3=C.WHITE},0.15)
        ind.Visible=true
    end)
    return tab
end

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
-- SISTEMA ANTI-TROLL
-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
local antiTrollEnabled = true
local antiTrollGlobal = true
local lastTrollAlert = {}
local detectionRadius = 4
local proximityTime = {}

local function canAlert(trollName, victimName)
    local key = trollName .. "->" .. victimName
    if not lastTrollAlert[key] then return true end
    return (tick() - lastTrollAlert[key]) > 5
end

Players.PlayerAdded:Connect(function(plr)
    plr.CharacterAdded:Connect(function(char)
        task.wait(0.5)
        local hum = char:WaitForChild("Humanoid", 5)
        if not hum then return end
        hum.Died:Connect(function()
            if not antiTrollEnabled then return end
            if plr == player and not antiTrollGlobal then return end
            task.wait(0.1)
            for _, otherPlayer in pairs(Players:GetPlayers()) do
                if otherPlayer ~= plr and otherPlayer.Character then
                    local otherHRP = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                    local victimHRP = char:FindFirstChild("HumanoidRootPart")
                    if otherHRP and victimHRP then
                        local distance = (otherHRP.Position - victimHRP.Position).Magnitude
                        if distance < 15 and canAlert(otherPlayer.Name, plr.Name) then
                            if plr == player then
                                notifyTroll(otherPlayer.Name, "Te matou ou fez resetar!")
                            else
                                notifyTroll(otherPlayer.Name, "Matou " .. plr.DisplayName .. "!")
                            end
                            lastTrollAlert[otherPlayer.Name .. "->" .. plr.Name] = tick()
                            break
                        end
                    end
                end
            end
        end)
    end)
end)

for _, plr in pairs(Players:GetPlayers()) do
    if plr.Character then
        task.spawn(function()
            local char = plr.Character
            task.wait(0.5)
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum then return end
            hum.Died:Connect(function()
                if not antiTrollEnabled then return end
                if plr == player and not antiTrollGlobal then return end
                task.wait(0.1)
                for _, otherPlayer in pairs(Players:GetPlayers()) do
                    if otherPlayer ~= plr and otherPlayer.Character then
                        local otherHRP = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                        local victimHRP = char:FindFirstChild("HumanoidRootPart")
                        if otherHRP and victimHRP then
                            local distance = (otherHRP.Position - victimHRP.Position).Magnitude
                            if distance < 15 and canAlert(otherPlayer.Name, plr.Name) then
                                if plr == player then
                                    notifyTroll(otherPlayer.Name, "Te matou ou fez resetar!")
                                else
                                    notifyTroll(otherPlayer.Name, "Matou " .. plr.DisplayName .. "!")
                                end
                                lastTrollAlert[otherPlayer.Name .. "->" .. plr.Name] = tick()
                                break
                            end
                        end
                    end
                end
            end)
        end)
    end
end

task.spawn(function()
    while task.wait(0.4) do
        if not antiTrollEnabled then continue end
        for _, victim in pairs(Players:GetPlayers()) do
            if not antiTrollGlobal and victim ~= player then continue end
            local victimChar = victim.Character
            if not victimChar then continue end
            local victimHRP = victimChar:FindFirstChild("HumanoidRootPart")
            local victimHum = victimChar:FindFirstChildOfClass("Humanoid")
            if not victimHRP or not victimHum then continue end
            for _, troll in pairs(Players:GetPlayers()) do
                if troll == victim then continue end
                local trollChar = troll.Character
                if not trollChar then continue end
                local trollHRP = trollChar:FindFirstChild("HumanoidRootPart")
                local trollHum = trollChar:FindFirstChildOfClass("Humanoid")
                if not trollHRP or not trollHum then continue end
                local distance = (trollHRP.Position - victimHRP.Position).Magnitude
                local heightDiff = trollHRP.Position.Y - victimHRP.Position.Y
                if heightDiff > 3 and heightDiff < 8 and distance < 5 then
                    if canAlert(troll.Name, victim.Name) then
                        if victim == player then
                            notifyTroll(troll.Name, "Pulou em cima de vocÃª!")
                        else
                            notifyTroll(troll.Name, "Pulou em " .. victim.DisplayName .. "!")
                        end
                        lastTrollAlert[troll.Name .. "->" .. victim.Name] = tick()
                    end
                end
                if distance < detectionRadius then
                    local key = troll.Name .. "_near_" .. victim.Name
                    if not proximityTime[key] then
                        proximityTime[key] = tick()
                    else
                        local timeNear = tick() - proximityTime[key]
                        if timeNear > 3 and canAlert(troll.Name, victim.Name) then
                            if victim == player then
                                notifyTroll(troll.Name, "EstÃ¡ grudado/perseguindo vocÃª!")
                            else
                                notifyTroll(troll.Name, "Perseguindo " .. victim.DisplayName .. "!")
                            end
                            lastTrollAlert[troll.Name .. "->" .. victim.Name] = tick()
                            proximityTime[key] = nil
                        end
                    end
                else
                    proximityTime[troll.Name .. "_near_" .. victim.Name] = nil
                end
                local victimVelocity = victimHRP.AssemblyLinearVelocity
                if victimVelocity.Magnitude > 50 and distance < 8 then
                    if canAlert(troll.Name, victim.Name) then
                        if victim == player then
                            notifyTroll(troll.Name, "Te empurrou/bateu em vocÃª!")
                        else
                            notifyTroll(troll.Name, "Empurrou " .. victim.DisplayName .. "!")
                        end
                        lastTrollAlert[troll.Name .. "->" .. victim.Name] = tick()
                    end
                end
            end
        end
    end
end)

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
-- ABA HOME
-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
local homeTab=newTab("Home","ðŸ ")
do
    local s=section(homeTab.page,"Bem-vindo")
    lbl(s,"AvenL Hub V20 - Brookhaven+ Edition\nUse as abas para navegar pelas funÃ§Ãµes.",
        UDim2.new(1,0,0,40),nil,11,C.DIM).LayoutOrder=2

    local s2=section(homeTab.page,"ðŸ‘‘ Equipe VIP")
    local ownerCard=Instance.new("Frame",s2)
    ownerCard.Size=UDim2.new(1,0,0,70); ownerCard.BackgroundColor3=C.GOLD
    ownerCard.LayoutOrder=2; corner(ownerCard,12); stroke(ownerCard,Color3.fromRGB(255,223,0),3)
    grad(ownerCard,Color3.fromRGB(255,215,0),Color3.fromRGB(255,180,0),45)
    lbl(ownerCard,"ðŸ‘‘",UDim2.new(0,50,0,50),UDim2.new(0,8,0,10),36,C.WHITE)
    lbl(ownerCard,"AVEN",UDim2.new(1,-70,0,28),UDim2.new(0,62,0,8),18,Color3.fromRGB(0,0,0),Enum.TextXAlignment.Left)
    lbl(ownerCard,"OWNER",UDim2.new(1,-70,0,24),UDim2.new(0,62,0,38),13,Color3.fromRGB(80,50,0),Enum.TextXAlignment.Left)

    local viceCard=Instance.new("Frame",s2)
    viceCard.Size=UDim2.new(1,0,0,65); viceCard.BackgroundColor3=C.GOLD
    viceCard.LayoutOrder=3; corner(viceCard,12); stroke(viceCard,Color3.fromRGB(255,200,0),3)
    grad(viceCard,Color3.fromRGB(255,200,0),Color3.fromRGB(255,160,0),45)
    lbl(viceCard,"â­",UDim2.new(0,45,0,45),UDim2.new(0,8,0,10),32,C.WHITE)
    lbl(viceCard,"MARIO",UDim2.new(1,-65,0,26),UDim2.new(0,58,0,8),16,Color3.fromRGB(0,0,0),Enum.TextXAlignment.Left)
    lbl(viceCard,"VICE-DONO",UDim2.new(1,-65,0,22),UDim2.new(0,58,0,36),12,Color3.fromRGB(80,50,0),Enum.TextXAlignment.Left)

    local s4=section(homeTab.page,"Utilidades")
    actionBtn(s4,"ðŸ”„ Rejoin Server",C.ACCENT1,2,function()
        game:GetService("TeleportService"):Teleport(game.PlaceId, player)
    end)

    local s5=section(homeTab.page,"ðŸ›¡ï¸ Sistema Anti-Troll")
    local statusLbl=lbl(s5,"Sistema: ATIVO âœ…",UDim2.new(1,0,0,24),nil,12,C.GREEN)
    statusLbl.LayoutOrder=2
    toggleBtn(s5,"ðŸ›¡ï¸ Anti-Troll: ON","âš ï¸ Anti-Troll: OFF",C.GREEN,C.RED,3,function(state)
        antiTrollEnabled=not state
        if antiTrollEnabled then
            statusLbl.Text="Sistema: ATIVO âœ…"; statusLbl.TextColor3=C.GREEN
            notify("Anti-Troll","Sistema reativado!",2)
        else
            statusLbl.Text="Sistema: DESATIVADO âš ï¸"; statusLbl.TextColor3=C.RED
            notify("Anti-Troll","Sistema desativado",2)
        end
    end)
    local globalLbl=lbl(s5,"Modo: GLOBAL ðŸŒ (todos)",UDim2.new(1,0,0,24),nil,12,C.ACCENT2)
    globalLbl.LayoutOrder=4
    toggleBtn(s5,"ðŸŒ Detectar em TODOS: ON","ðŸ‘¤ SÃ³ em VOCÃŠ: OFF",C.ACCENT2,C.ORANGE,5,function(state)
        antiTrollGlobal=not state
        if antiTrollGlobal then
            globalLbl.Text="Modo: GLOBAL ðŸŒ (todos)"; globalLbl.TextColor3=C.ACCENT2
            notify("Anti-Troll","Detectando trolls em TODO MUNDO!",2)
        else
            globalLbl.Text="Modo: PESSOAL ðŸ‘¤ (sÃ³ vocÃª)"; globalLbl.TextColor3=C.ORANGE
            notify("Anti-Troll","Detectando sÃ³ em vocÃª",2)
        end
    end)
    local infoLbl=lbl(s5,"ðŸŽ¯ MODO GLOBAL:\nVocÃª vÃª quando QUALQUER pessoa estÃ¡ sendo trollada!\n\nðŸ” O sistema detecta:\nâ€¢ Pulos em cima\nâ€¢ PerseguiÃ§Ã£o/grudando\nâ€¢ EmpurrÃµes e colisÃµes\nâ€¢ Quando alguÃ©m mata outro",
        UDim2.new(1,0,0,110),nil,10,C.DIM)
    infoLbl.TextWrapped=true; infoLbl.LayoutOrder=6
end

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
-- ABA PLAYER
-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
local playerTab=newTab("Player","ðŸ§")
do
    local s=section(playerTab.page,"Velocidade")
    local speed=16
    local speedLbl=lbl(s,"âš¡ "..speed,UDim2.new(1,0,0,28),nil,13,C.WHITE); speedLbl.LayoutOrder=2
    local function applySpeed()
        local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed=speed end
        speedLbl.Text="âš¡ "..speed
    end
    local r=Instance.new("Frame",s)
    r.Size=UDim2.new(1,0,0,32); r.BackgroundTransparency=1; r.LayoutOrder=3
    btn(r,"-10",UDim2.new(0,50,1,0),UDim2.new(0,0,0,0),C.RED).MouseButton1Click:Connect(function() speed=math.max(5,speed-10); applySpeed() end)
    btn(r,"+10",UDim2.new(0,50,1,0),UDim2.new(1,-50,0,0),C.GREEN).MouseButton1Click:Connect(function() speed=math.min(1000,speed+10); applySpeed() end)
    local s2=section(playerTab.page,"Extras")
    local noclip=false
    toggleBtn(s2,"ðŸš« Noclip: OFF","âœ… Noclip: ON",C.CARD,C.GREEN,2,function(st) noclip=st end)
    RunService.Stepped:Connect(function()
        if noclip and player.Character then
            for _,v in pairs(player.Character:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide=false end
            end
        end
    end)
end

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
-- ABA MÃšSICA
-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
local musicTab=newTab("MÃºsica","ðŸŽµ")
do
    local playlists = {
        ["ðŸ†• Novos 2025/2026"] = {
            {name="Perigosa (Repost)",           id="993169998084000"},
            {name="Assombra Matrix 9",           id="130149486547895"},
            {name="Cocota Nesgreco",             id="130758596227702"},
            {name="Mandela SP",                  id="137970023957031"},
            {name="Casal Mandrake",              id="4890709034"},
            {name="Som do ParedÃ£o",              id="6490661033"},
            {name="MC Don Juan - Confia Amiga",  id="2700449900"},
            {name="MC Zaquin - Ã” MoÃ§a",          id="6333384941"},
            {name="MC Menor ZL - Bandida",       id="6309420461"},
            {name="MC Xenon - Ã‰ De MÃ³ TempÃ£o",   id="5991269354"},
            {name="MC Nem JM - Umbrela",         id="6304385217"},
            {name="MC Marks - Jesus Olhou",      id="6257239545"},
            {name="MC Davi part Kevin - BailÃ£o", id="2723422995"},
            {name="MC Luan - Primeira Vista",    id="6549552704"},
        },
        ["ðŸ”¥ Funk BR"] = {
            {name="Baile de Favela",             id="7254566675"},
            {name="Olha a ExplosÃ£o",             id="1132154508"},
            {name="Envolvimento - MC Loma",      id="6117112610"},
            {name="Toma Vapo Vapo - ZÃ© Felipe",  id="8175801924"},
            {name="SentadÃ£o - Pedro Sampaio",    id="4572496137"},
            {name="Amor ou o LitrÃ£o",            id="6117357206"},
            {name="MC Kevin - Doutora 3",        id="7017730438"},
            {name="MC Topre - Apaga Luz Tudo",   id="6318830246"},
            {name="TamborzÃ£o",                   id="7020015580"},
            {name="MC Leozin - Esqueci Namorar", id="4227656576"},
            {name="MC Frog - Golpista",          id="6254649803"},
            {name="MC Surtada",                  id="3838634748"},
            {name="Funk Chora Mais",             id="1024906897"},
            {name="MC Niack - Oh Juliana",       id="5794385002"},
            {name="Mc JoÃ£o - Baile de Favela V", id="1841682637"},
        },
        ["ðŸŽµ Montagem/MTG"] = {
            {name="Montagem Mysterious Game",    id="1847850505"},
            {name="Montagem Miau",               id="89519222614780"},
            {name="Automotivo Bibi Fogosa",      id="9046862941"},
            {name="MTG Quem NÃ£o Quer",           id="9042781417"},
            {name="Montagem Matar o Beat",       id="5962594796"},
            {name="VisÃ£o de Futuro",             id="6043914200"},
            {name="Chama No Zoom Close",         id="7714150300"},
            {name="Baile da EscÃ³cia",            id="5831100035"},
        },
        ["ðŸ¤  Piseiro/ForrÃ³"] = {
            {name="BarÃµes da Pisadinha",         id="121187736532042"},
            {name="Toca o Trompete",             id="6918766819"},
            {name="Recairei",                    id="6535162330"},
            {name="GalÃ£ do Momento",             id="7251056536"},
            {name="Vai Tubo Arrocha",            id="6918469685"},
            {name="Pablo - Nosso Primeiro Beijo",id="6795482154"},
        },
        ["ðŸ“± Phonk"] = {
            {name="Murder in My Mind",           id="6891418142"},
            {name="Cowboys",                     id="1845756489"},
            {name="Funk Estranho",               id="6350828251"},
            {name="Brazilian Phonk",             id="8663653977"},
            {name="DJ Sasori - Beat Triste",     id="7123596246"},
        },
        ["ðŸŽ¤ Trap/Rap BR"] = {
            {name="GATA ONLY",                   id="6903302049"},
            {name="MatuÃª - MÃ¡quina do Tempo",    id="7071495474"},
            {name="Wiu - Rainha da Finesse",     id="7060571017"},
            {name="MalvadÃ£o 3",                  id="7487607735"},
            {name="Nego Drama - Racionais",      id="7819563881"},
            {name="Rei Lacoste - MD Chefe",      id="7202437832"},
            {name="PlutÃ£o - VMZ",                id="6736643454"},
            {name="MC Poze - To Voando Alto",    id="4432916296"},
            {name="MC Luan - Outras Vidas",      id="6520493218"},
            {name="MC Luan - Redes Sociais",     id="6549522717"},
        },
        ["ðŸ’” SofrÃªncia"] = {
            {name="MarÃ­lia MendonÃ§a Funk",       id="4508655819"},
            {name="MC Anjim - Bala Love",        id="7319827986"},
            {name="MC Lipi - JÃ¡ Chorei Demais",  id="6740898532"},
            {name="MC Charada - Chorar Pra Que", id="2699365781"},
            {name="Nivy - Quinto Degrau",        id="8078445184"},
            {name="MC Juninho - Se For Sera",    id="1343187961"},
            {name="MC 3L - Um SabadÃ£o Desse",    id="6052891776"},
        },
        ["ðŸŽ¸ Rock"] = {
            {name="Bohemian Rhapsody",           id="2564781070"},
            {name="Sweet Child O Mine",          id="5142893277"},
            {name="Stairway to Heaven",          id="1848889282"},
        },
        ["ðŸŽ§ EletrÃ´nica"] = {
            {name="Faded - Alan Walker",         id="572129801"},
            {name="Alone - Marshmello",          id="887600872"},
            {name="Animals - Martin Garrix",     id="155693589"},
            {name="Titanium",                    id="155762585"},
            {name="Ameno Funk (Dorime)",         id="888328110"},
        },
        ["ðŸ˜‚ Memes/Zoeira"] = {
            {name="Funk do Sub-Zero MK",         id="4282646399"},
            {name="R.R.Soares Funk Jesus",       id="1363540130"},
            {name="Vai Malandro",                id="4680884601"},
            {name="Funk Bella Ciao",             id="1583528384"},
            {name="Suco de MaracujÃ¡ Funk",       id="1036579690"},
            {name="Funk do Chaves",              id="848130345"},
            {name="Funk Rei LeÃ£o",               id="3821334095"},
            {name="Funk das Winxs",              id="895438736"},
            {name="Bom Dia Consagrado Funk",     id="2621647660"},
            {name="Xuxa - Soco Bate Bira Funk",  id="2284321900"},
            {name="Funk Naruto Triste",          id="1472963352"},
            {name="Funk Game of Thrones",        id="1216648549"},
            {name="Funk do Rei LeÃ£o",            id="3821334095"},
            {name="Funk dos Beckyardigans",      id="1046166036"},
        },
        ["ðŸŽ¹ ClÃ¡ssicas"] = {
            {name="Fur Elise",                   id="110648703594220"},
            {name="Moonlight Sonata",            id="1837879082"},
            {name="Canon in D",                  id="1848879982"},
            {name="Clair de Lune",               id="1848031213"},
            {name="Spring - Vivaldi",            id="5254276207"},
        },
    }

    local s_info = section(musicTab.page, "â„¹ï¸ Como Usar")
    local infoLbl = lbl(s_info, "ðŸ“‹ Clique no ID para COPIAR\nðŸŽµ Use os IDs no Boombox do jogo ou no Radio",
        UDim2.new(1, 0, 0, 40), nil, 11, C.DIM)
    infoLbl.TextWrapped = true; infoLbl.LayoutOrder = 2

    for styleName, songs in pairs(playlists) do
        local s = section(musicTab.page, "ðŸŽµ " .. styleName)
        for i, song in ipairs(songs) do
            local songFrame = Instance.new("Frame", s)
            songFrame.Size = UDim2.new(1, 0, 0, 40)
            songFrame.BackgroundColor3 = C.BG
            songFrame.LayoutOrder = i + 1
            corner(songFrame, 8); stroke(songFrame, C.BORDER, 1)
            lbl(songFrame, "ðŸŽµ " .. song.name, UDim2.new(0.6, -10, 1, 0), UDim2.new(0, 10, 0, 0), 11, C.TEXT, Enum.TextXAlignment.Left)
            local copyBtn = btn(songFrame, "ðŸ“‹ " .. song.id, UDim2.new(0.4, -10, 0, 28), UDim2.new(0.6, 5, 0.5, -14), C.ACCENT1, 10)
            copyBtn.MouseButton1Click:Connect(function()
                setclipboard(song.id)
                notify("Copiado!", song.name .. " - ID copiado!", 2)
                tween(copyBtn, {BackgroundColor3 = C.GREEN}, 0.2)
                task.wait(0.5)
                tween(copyBtn, {BackgroundColor3 = C.ACCENT1}, 0.3)
            end)
        end
    end

    local s_custom = section(musicTab.page, "âž• MÃºsica Personalizada")
    local customNameBox = textbox(s_custom, "Nome da mÃºsica...", 2)
    local customIdBox = textbox(s_custom, "ID da mÃºsica...", 3)
    actionBtn(s_custom, "ðŸ“‹ Copiar ID Personalizado", C.ACCENT2, 4, function()
        if customIdBox.Text ~= "" then
            setclipboard(customIdBox.Text)
            notify("Copiado!", "ID copiado!", 2)
        end
    end)
    actionBtn(s_custom, "ðŸ—‘ï¸ Limpar Campos", C.RED, 5, function()
        customNameBox.Text = ""; customIdBox.Text = ""
    end)
end

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
-- ABA BH+
-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
local bhTab=newTab("BH+","ðŸ¡")
do
    local selectedObj = nil
    local function spawnWall()
        local char = player.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        local wall = Instance.new("Part")
        wall.Name = "AvenWall"; wall.Size = Vector3.new(14, 10, 1)
        wall.Position = char.HumanoidRootPart.Position + char.HumanoidRootPart.CFrame.LookVector * 8
        wall.Anchored = true; wall.Material = Enum.Material.SmoothPlastic
        wall.Color = Color3.fromRGB(math.random(50,255), math.random(50,255), math.random(50,255))
        wall.Parent = workspace; selectedObj = wall
        notify("Build", "Parede criada!", 2)
    end
    local function spawnCarpet()
        local char = player.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        local carpet = Instance.new("Part")
        carpet.Name = "AvenCarpet"; carpet.Size = Vector3.new(20, 0.5, 20)
        carpet.Position = char.HumanoidRootPart.Position + Vector3.new(0, -3, 0)
        carpet.Anchored = true; carpet.Material = Enum.Material.Fabric
        carpet.Color = Color3.fromRGB(math.random(50,255), math.random(50,255), math.random(50,255))
        carpet.Parent = workspace; selectedObj = carpet
        notify("Build", "Tapete criado!", 2)
    end

    local s_build = section(bhTab.page, "ðŸ—ï¸ Build")
    actionBtn(s_build, "ðŸ§± Criar Parede", C.ORANGE, 2, spawnWall)
    actionBtn(s_build, "ðŸŸ« Criar Tapete", C.CARD, 3, spawnCarpet)
    actionBtn(s_build, "ðŸŽ¨ Cor AleatÃ³ria", Color3.fromRGB(160,60,200), 4, function()
        if selectedObj and selectedObj.Parent then
            selectedObj.Color = Color3.fromRGB(math.random(50,255), math.random(50,255), math.random(50,255))
            notify("Build", "Cor alterada!", 1)
        end
    end)
    actionBtn(s_build, "ðŸ—‘ï¸ Deletar", C.RED, 5, function()
        if selectedObj and selectedObj.Parent then
            selectedObj:Destroy(); selectedObj = nil
            notify("Build", "Deletado!", 2)
        end
    end)

    local s_save = section(bhTab.page, "ðŸ’¾ Salvar & Carregar")
    local buildCodeBox = textbox(s_save, "Cole o cÃ³digo aqui...", 2)
    actionBtn(s_save, "ðŸ’¾ Gerar CÃ³digo", C.ACCENT1, 3, function()
        if selectedObj and selectedObj.Parent then
            local p = selectedObj.Position
            local code = string.format("BUILD|%.1f,%.1f,%.1f|%d,%d,%d",
                p.X, p.Y, p.Z,
                math.floor(selectedObj.Color.R*255),
                math.floor(selectedObj.Color.G*255),
                math.floor(selectedObj.Color.B*255))
            buildCodeBox.Text = code; setclipboard(code)
            notify("Salvo!", "CÃ³digo copiado!", 2)
        end
    end)
    actionBtn(s_save, "ðŸ“¥ Carregar CÃ³digo", C.GREEN, 4, function()
        local parts = string.split(buildCodeBox.Text, "|")
        if #parts >= 3 and parts[1] == "BUILD" then
            local pos = string.split(parts[2], ",")
            local obj = Instance.new("Part")
            obj.Size = Vector3.new(14, 10, 1)
            obj.Position = Vector3.new(tonumber(pos[1]), tonumber(pos[2]), tonumber(pos[3]))
            obj.Anchored = true; obj.Parent = workspace
            notify("Build", "Carregado!", 2)
        end
    end)

    local selectedPlayer = nil
    local s1 = section(bhTab.page, "ðŸ‘¤ Selecionar Jogador")
    local playerBox = textbox(s1, "Digite o nome do jogador...", 2)
    actionBtn(s1, "âœ… Confirmar Jogador", C.ACCENT1, 3, function()
        if playerBox.Text ~= "" then
            selectedPlayer = playerBox.Text
            notify("Player", selectedPlayer .. " selecionado!", 2)
        end
    end)

    local s2 = section(bhTab.page, "ðŸŽ¯ AÃ§Ãµes")
    actionBtn(s2, "ðŸ‘ï¸ View Jogador", C.ACCENT1, 2, function()
        if not selectedPlayer then notify("Erro", "Selecione um jogador!", 2); return end
        local target = Players:FindFirstChild(selectedPlayer)
        if target and target.Character and target.Character:FindFirstChild("Humanoid") then
            workspace.CurrentCamera.CameraSubject = target.Character.Humanoid
            notify("View", "Vendo: " .. selectedPlayer, 2)
        end
    end)
    actionBtn(s2, "ðŸ”™ Voltar View", C.CARD, 3, function()
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            workspace.CurrentCamera.CameraSubject = player.Character.Humanoid
            notify("View", "Voltou!", 2)
        end
    end)
    actionBtn(s2, "ðŸš€ Teleport para Jogador", C.ACCENT2, 4, function()
        if not selectedPlayer then notify("Erro", "Selecione um jogador!", 2); return end
        local target = Players:FindFirstChild(selectedPlayer)
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            local myChar = player.Character
            if myChar and myChar:FindFirstChild("HumanoidRootPart") then
                myChar.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
                notify("TP", "Teleportado!", 2)
            end
        end
    end)
    actionBtn(s2, "ðŸš— Car Bring (Trazer)", C.YELLOW, 5, function()
        if not selectedPlayer then notify("Erro", "Selecione um jogador!", 2); return end
        notify("Car Bring", "Iniciando...", 3)
        task.spawn(function()
            pcall(function()
                local Vehicles = Workspace:FindFirstChild("Vehicles")
                if not Vehicles then return end
                local myChar = player.Character
                if not myChar then return end
                local HRP = myChar:FindFirstChild("HumanoidRootPart")
                local Hum = myChar:FindFirstChildOfClass("Humanoid")
                if not HRP or not Hum then return end
                local PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                if not PCar then
                    HRP.CFrame = CFrame.new(1118.81, 75.998, -1138.61)
                    task.wait(0.5)
                    local remote = ReplicatedStorage:FindFirstChild("RE"):FindFirstChild("1Ca1r")
                    if remote then remote:FireServer("PickingCar", "SchoolBus"); task.wait(0.5) end
                    PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                end
                if PCar then
                    local Seat = PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
                    if Seat and not Hum.Sit then
                        repeat task.wait(); HRP.CFrame = Seat.CFrame until Hum.Sit
                    end
                    local target = Players:FindFirstChild(selectedPlayer)
                    if target and target.Character then
                        local TargetRP = target.Character:FindFirstChild("HumanoidRootPart")
                        local TargetH = target.Character:FindFirstChildOfClass("Humanoid")
                        if TargetRP and TargetH and not TargetH.Sit then
                            local attempts = 0
                            while not TargetH.Sit and attempts < 100 do
                                attempts = attempts + 1; task.wait()
                                if PCar and PCar.PrimaryPart then
                                    PCar:SetPrimaryPartCFrame(CFrame.new(TargetRP.Position) * CFrame.new(0,1.5,0) * CFrame.Angles(math.rad(attempts*20),0,0))
                                end
                            end
                            notify("Car Bring", "Jogador capturado!", 2)
                        end
                    end
                end
            end)
        end)
    end)
    actionBtn(s2, "ðŸ’€ Car Kill (Matar)", C.RED, 6, function()
        if not selectedPlayer then notify("Erro", "Selecione um jogador!", 2); return end
        notify("Car Kill", "Iniciando...", 2)
        task.spawn(function()
            pcall(function()
                local Vehicles = Workspace:FindFirstChild("Vehicles")
                if not Vehicles then return end
                local myChar = player.Character
                if not myChar then return end
                local HRP = myChar:FindFirstChild("HumanoidRootPart")
                local Hum = myChar:FindFirstChildOfClass("Humanoid")
                if not HRP or not Hum then return end
                local OldPos = HRP.CFrame
                local PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                if not PCar then
                    HRP.CFrame = CFrame.new(1118.81, 75.998, -1138.61)
                    task.wait(0.5)
                    local remote = ReplicatedStorage:FindFirstChild("RE"):FindFirstChild("1Ca1r")
                    if remote then remote:FireServer("PickingCar", "SchoolBus"); task.wait(0.5) end
                    PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                end
                if PCar then
                    local Seat = PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
                    if Seat and not Hum.Sit then
                        repeat task.wait(); HRP.CFrame = Seat.CFrame until Hum.Sit
                    end
                    local target = Players:FindFirstChild(selectedPlayer)
                    if target and target.Character then
                        local TargetRP = target.Character:FindFirstChild("HumanoidRootPart")
                        local TargetH = target.Character:FindFirstChildOfClass("Humanoid")
                        if TargetRP and TargetH and not TargetH.Sit then
                            while not TargetH.Sit and TargetH.Health > 0 do
                                task.wait()
                                if PCar and PCar.PrimaryPart then
                                    PCar:SetPrimaryPartCFrame(CFrame.new(TargetRP.Position) * CFrame.new(0,1.5,0) * CFrame.Angles(math.rad(math.random(-360,360)),math.rad(math.random(-360,360)),math.rad(math.random(-360,360))))
                                end
                            end
                            if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(0,-500,0)) end
                            task.wait(0.2); Hum.Sit = false; task.wait(0.1); HRP.CFrame = OldPos
                            notify("Car Kill", "ConcluÃ­do!", 2)
                        end
                    end
                end
            end)
        end)
    end)

    -- Ã”nibus destinos
    local s3 = section(bhTab.page, "ðŸšŒ Ã”nibus - Destinos")
    local busDestBox = textbox(s3, "Ex: 1, 2, 3, supermercado, parque...", 2)
    local busCodeBox = textbox(s3, "CÃ³digo do destino...", 3)
    local busDestinations = {
        ["1"]=Vector3.new(-50,5,-30), ["2"]=Vector3.new(-25,5,-30), ["3"]=Vector3.new(0,5,-30),
        ["supermercado"]=Vector3.new(-80,5,-70), ["parque"]=Vector3.new(80,5,70),
        ["centro"]=Vector3.new(0,5,0), ["escola"]=Vector3.new(50,5,-50), ["hospital"]=Vector3.new(-60,5,60),
    }
    actionBtn(s3, "ðŸšŒ Enviar Ã”nibus para Destino", C.YELLOW, 4, function()
        if not selectedPlayer then notify("Erro", "Selecione um jogador!", 2); return end
        local destPos = busDestinations[string.lower(busDestBox.Text)]
        if not destPos then notify("Erro", "Destino invÃ¡lido!", 2); return end
        notify("Ã”nibus", "Enviando...", 2)
        task.spawn(function()
            pcall(function()
                local Vehicles = Workspace:FindFirstChild("Vehicles")
                if not Vehicles then return end
                local myChar = player.Character
                if not myChar then return end
                local HRP = myChar:FindFirstChild("HumanoidRootPart")
                local Hum = myChar:FindFirstChildOfClass("Humanoid")
                if not HRP or not Hum then return end
                local PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                if not PCar then
                    HRP.CFrame = CFrame.new(1118.81, 75.998, -1138.61); task.wait(0.5)
                    local remote = ReplicatedStorage:FindFirstChild("RE"):FindFirstChild("1Ca1r")
                    if remote then remote:FireServer("PickingCar", "SchoolBus"); task.wait(0.5) end
                    PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                end
                if PCar then
                    local Seat = PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
                    if Seat and not Hum.Sit then repeat task.wait(); HRP.CFrame = Seat.CFrame until Hum.Sit end
                    task.wait(0.3)
                    local target = Players:FindFirstChild(selectedPlayer)
                    if target and target.Character then
                        local TargetRP = target.Character:FindFirstChild("HumanoidRootPart")
                        local TargetH = target.Character:FindFirstChildOfClass("Humanoid")
                        if TargetRP and TargetH and not TargetH.Sit then
                            local attempts = 0
                            while not TargetH.Sit and attempts < 80 do
                                attempts = attempts + 1; task.wait()
                                if PCar and PCar.PrimaryPart then
                                    PCar:SetPrimaryPartCFrame(CFrame.new(TargetRP.Position) * CFrame.new(0,1.5,0) * CFrame.Angles(math.rad(attempts*15),0,0))
                                end
                            end
                            if TargetH.Sit then
                                task.wait(0.5)
                                if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(destPos)) end
                                task.wait(1); notify("Ã”nibus", "Chegou! ðŸŽ¯", 3)
                            end
                        end
                    end
                end
            end)
        end)
    end)
    actionBtn(s3, "ðŸ  Casa Embaixo (Y-35)", Color3.fromRGB(100,150,255), 5, function()
        if not selectedPlayer then notify("Erro", "Selecione um jogador!", 2); return end
        local destPos = busDestinations[string.lower(busDestBox.Text)]
        if not destPos then notify("Erro", "Destino invÃ¡lido!", 2); return end
        local destPosBaixo = Vector3.new(destPos.X, destPos.Y - 35, destPos.Z)
        notify("Ã”nibus", "Indo para BAIXO da casa...", 2)
        task.spawn(function()
            pcall(function()
                local Vehicles = Workspace:FindFirstChild("Vehicles")
                if not Vehicles then return end
                local myChar = player.Character
                if not myChar then return end
                local HRP = myChar:FindFirstChild("HumanoidRootPart")
                local Hum = myChar:FindFirstChildOfClass("Humanoid")
                if not HRP or not Hum then return end
                local PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                if not PCar then
                    HRP.CFrame = CFrame.new(1118.81, 75.998, -1138.61); task.wait(0.5)
                    local remote = ReplicatedStorage:FindFirstChild("RE"):FindFirstChild("1Ca1r")
                    if remote then remote:FireServer("PickingCar", "SchoolBus"); task.wait(0.5) end
                    PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                end
                if PCar then
                    local Seat = PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
                    if Seat and not Hum.Sit then repeat task.wait(); HRP.CFrame = Seat.CFrame until Hum.Sit end
                    task.wait(0.3)
                    local target = Players:FindFirstChild(selectedPlayer)
                    if target and target.Character then
                        local TargetRP = target.Character:FindFirstChild("HumanoidRootPart")
                        local TargetH = target.Character:FindFirstChildOfClass("Humanoid")
                        if TargetRP and TargetH and not TargetH.Sit then
                            local attempts = 0
                            while not TargetH.Sit and attempts < 80 do
                                attempts = attempts + 1; task.wait()
                                if PCar and PCar.PrimaryPart then
                                    PCar:SetPrimaryPartCFrame(CFrame.new(TargetRP.Position) * CFrame.new(0,1.5,0) * CFrame.Angles(math.rad(attempts*15),0,0))
                                end
                            end
                            if TargetH.Sit then
                                task.wait(0.5)
                                if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(destPosBaixo)) end
                                task.wait(1); notify("Ã”nibus", "Chegou na parte de BAIXO! ðŸ â¬‡ï¸", 3)
                            end
                        end
                    end
                end
            end)
        end)
    end)
    actionBtn(s3, "ðŸ’¾ Salvar Destino Atual", C.ACCENT1, 6, function()
        local destInput = string.lower(busDestBox.Text)
        local destPos = busDestinations[destInput]
        if destPos then
            local code = string.format("BUS|%s|%.1f,%.1f,%.1f", destInput, destPos.X, destPos.Y, destPos.Z)
            busCodeBox.Text = code; setclipboard(code)
            notify("Salvo!", "CÃ³digo copiado! ðŸ“‹", 2)
        end
    end)
    actionBtn(s3, "ðŸ“¥ Carregar do CÃ³digo", C.GREEN, 7, function()
        local parts = string.split(busCodeBox.Text, "|")
        if #parts >= 3 and parts[1] == "BUS" then
            busDestBox.Text = parts[2]
            notify("Carregado!", "Destino: " .. parts[2], 2)
        end
    end)
    actionBtn(s3, "ðŸ“ Salvar PosiÃ§Ã£o Atual", C.ORANGE, 8, function()
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local pos = char.HumanoidRootPart.Position
            local code = string.format("BUS|customizada|%.1f,%.1f,%.1f", pos.X, pos.Y, pos.Z)
            busCodeBox.Text = code; setclipboard(code)
            notify("Salvo!", "PosiÃ§Ã£o atual salva! ðŸ“Œ", 2)
        end
    end)

    -- Kill Bus 02
    local s4 = section(bhTab.page, "ðŸ’€ Kill Bus 02 - Passagem Secreta")
    local killBusBox = textbox(s4, "Nome do jogador (ou seu nome para ir junto)", 2)
    local listFrame2 = Instance.new("Frame", s4)
    listFrame2.Size = UDim2.new(1, 0, 0, 0); listFrame2.AutomaticSize = Enum.AutomaticSize.Y
    listFrame2.BackgroundTransparency = 1; listFrame2.LayoutOrder = 3
    local listLayout2 = Instance.new("UIListLayout", listFrame2); listLayout2.Padding = UDim.new(0, 3)
    local function refreshKillBusList()
        for _, v in pairs(listFrame2:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
        for _, p in pairs(Players:GetPlayers()) do
            local pBtn = btn(listFrame2, "ðŸŽ¯ " .. p.Name, UDim2.new(1,0,0,28), nil, C.CARD, 11)
            pBtn.TextXAlignment = Enum.TextXAlignment.Left
            pBtn.MouseButton1Click:Connect(function() killBusBox.Text = p.Name; notify("Selecionado", p.Name, 1) end)
        end
    end
    actionBtn(s4, "ðŸ”„ Atualizar Lista", C.CARD, 4, refreshKillBusList)
    refreshKillBusList()
    actionBtn(s4, "ðŸ’€ Kill Bus 02 - Passagem Secreta", Color3.fromRGB(150,0,200), 5, function()
        local targetName = killBusBox.Text
        if targetName == "" then notify("Erro", "Digite ou selecione um jogador!", 2); return end
        local goTogether = (targetName == player.Name or targetName == player.DisplayName)
        if goTogether then notify("Kill Bus 02", "VocÃª vai JUNTO! ðŸšŒ", 3)
        else notify("Kill Bus 02", "Enviando " .. targetName .. "...", 3) end
        task.spawn(function()
            pcall(function()
                local Character = player.Character
                if not Character then return end
                local Humanoid = Character:FindFirstChildOfClass("Humanoid")
                local RootPart = Character:FindFirstChild("HumanoidRootPart")
                local Vehicles = Workspace:FindFirstChild("Vehicles")
                if not Humanoid or not RootPart or not Vehicles then return end
                local OldPos = RootPart.CFrame
                local PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                if not PCar then
                    RootPart.CFrame = CFrame.new(1118.81, 75.998, -1138.61); task.wait(0.5)
                    local remote = ReplicatedStorage:FindFirstChild("RE")
                    if remote then remote = remote:FindFirstChild("1Ca1r")
                        if remote then remote:FireServer("PickingCar", "SchoolBus"); task.wait(0.5) end
                    end
                    PCar = Vehicles:FindFirstChild(player.Name .. "Car")
                end
                if not PCar then notify("Erro", "NÃ£o conseguiu spawnar Ã´nibus!", 3); return end
                local Seat = PCar:FindFirstChild("Body")
                if Seat then Seat = Seat:FindFirstChild("VehicleSeat") end
                if Seat then
                    repeat task.wait(); RootPart.CFrame = Seat.CFrame * CFrame.new(0, math.random(-1,1), 0) until Humanoid.Sit or not Seat.Parent
                end
                task.wait(0.3)
                if goTogether then
                    if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(Vector3.new(71,-84,67))) end
                    task.wait(1); notify("Kill Bus 02", "Chegou na PASSAGEM SECRETA! ðŸ—ï¸", 3)
                else
                    local TargetPlayer = Players:FindFirstChild(targetName)
                    if not TargetPlayer then
                        notify("Erro", "Jogador nÃ£o encontrado!", 2); Humanoid.Sit = false; task.wait(0.1); RootPart.CFrame = OldPos; return
                    end
                    local TargetC = TargetPlayer.Character
                    local TargetH = TargetC and TargetC:FindFirstChildOfClass("Humanoid")
                    local TargetRP = TargetC and TargetC:FindFirstChild("HumanoidRootPart")
                    if TargetC and TargetH and TargetRP and not TargetH.Sit then
                        local attempts = 0
                        while not TargetH.Sit and attempts < 120 and TargetH.Health > 0 do
                            attempts = attempts + 1; task.wait()
                            if not PCar or not PCar.PrimaryPart then break end
                            local rX,rY,rZ = math.random(-360,360),math.random(-360,360),math.random(-360,360)
                            local killFn = function(pos,offset,ang) pcall(function() PCar:SetPrimaryPartCFrame(CFrame.new(pos.Position)*offset*ang) end) end
                            killFn(TargetRP,CFrame.new(0,1,0)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                            killFn(TargetRP,CFrame.new(0,-2.25,5)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                            killFn(TargetRP,CFrame.new(0,2.25,0.25)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.10,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                            killFn(TargetRP,CFrame.new(-2.25,-1.5,2.25)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.10,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                        end
                        if TargetH.Sit then
                            task.wait(0.5)
                            if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(Vector3.new(71,-84,67))) end
                            task.wait(1)
                            if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(0,-470,0)) end
                            task.wait(0.2); Humanoid.Sit = false; task.wait(0.1); RootPart.CFrame = OldPos
                            notify("Kill Bus 02", targetName .. " estÃ¡ na PASSAGEM! ðŸŽ¯", 3)
                        else
                            notify("Kill Bus 02", "NÃ£o conseguiu capturar!", 2)
                            Humanoid.Sit = false; task.wait(0.1); RootPart.CFrame = OldPos
                        end
                    end
                end
            end)
        end)
    end)
end

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
-- ABA GAMES - BROOKHAVEN+ HUB COMPLETO
-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
local gamesTab=newTab("GAMES","ðŸŽ®")
do
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- HEADER ANIMADO
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local headerCard = Instance.new("Frame", gamesTab.page)
    headerCard.Size = UDim2.new(1,-6,0,50)
    headerCard.BackgroundColor3 = Color3.fromRGB(18,8,32)
    headerCard.LayoutOrder = 0
    corner(headerCard, 12); stroke(headerCard, Color3.fromRGB(120,70,255), 2)
    grad(headerCard, Color3.fromRGB(30,10,55), Color3.fromRGB(15,5,30), 90)
    local headerLbl = lbl(headerCard, "âš¡ BROOKHAVEN+ HUB âš¡", UDim2.new(1,0,1,0), UDim2.new(0,0,0,0), 14, Color3.fromRGB(180,80,255))
    headerLbl.Font = Enum.Font.GothamBold
    task.spawn(function()
        while headerCard and headerCard.Parent do
            tween(headerLbl,{TextColor3=Color3.fromRGB(220,130,255)},0.5,Enum.EasingStyle.Sine)
            task.wait(0.5)
            tween(headerLbl,{TextColor3=Color3.fromRGB(120,30,200)},0.5,Enum.EasingStyle.Sine)
            task.wait(0.5)
        end
    end)

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- IMAGEM DO BROOKHAVEN + BOTÃƒO
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhImgCard = Instance.new("Frame", gamesTab.page)
    bhImgCard.Size = UDim2.new(1,-6,0,80)
    bhImgCard.BackgroundColor3 = Color3.fromRGB(12,12,20)
    bhImgCard.LayoutOrder = 1
    corner(bhImgCard, 10); stroke(bhImgCard, Color3.fromRGB(50,205,50), 1.5)

    -- Imagem do Brookhaven (thumbnail oficial)
    local bhThumb = Instance.new("ImageLabel", bhImgCard)
    bhThumb.Size = UDim2.new(0,120,1,-8)
    bhThumb.Position = UDim2.new(0,4,0,4)
    bhThumb.Image = "rbxassetid://5672420098"  -- Brookhaven RP thumbnail
    bhThumb.BackgroundTransparency = 1
    bhThumb.ScaleType = Enum.ScaleType.Crop
    corner(bhThumb, 8)

    local bhTitleLbl = lbl(bhImgCard, "ðŸ¡ BROOKHAVEN RP", UDim2.new(1,-135,0,22), UDim2.new(0,130,0,6), 13, Color3.fromRGB(60,255,100), Enum.TextXAlignment.Left)
    bhTitleLbl.Font = Enum.Font.GothamBold
    local bhSubLbl = lbl(bhImgCard, "Hub de Scripts & FunÃ§Ãµes\nGame ID: 4924922222", UDim2.new(1,-135,0,32), UDim2.new(0,130,0,28), 10, Color3.fromRGB(140,200,140), Enum.TextXAlignment.Left)
    bhSubLbl.TextWrapped = true

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- BOTÃƒO PRINCIPAL BROOKHAVEN+
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhBtn = Instance.new("TextButton", gamesTab.page)
    bhBtn.Size = UDim2.new(1,-6,0,50)
    bhBtn.BackgroundColor3 = Color3.fromRGB(20,80,25)
    bhBtn.Text = "ðŸ¡  ABRIR BROOKHAVEN+ HUB"
    bhBtn.TextSize = 14; bhBtn.Font = Enum.Font.GothamBold
    bhBtn.TextColor3 = Color3.fromRGB(60,255,100)
    bhBtn.AutoButtonColor = false; bhBtn.LayoutOrder = 2
    corner(bhBtn, 10)
    grad(bhBtn, Color3.fromRGB(25,100,30), Color3.fromRGB(12,55,18), 90)
    local bhBtnStroke = Instance.new("UIStroke", bhBtn)
    bhBtnStroke.Color = Color3.fromRGB(50,205,50); bhBtnStroke.Thickness = 2
    bhBtn.MouseEnter:Connect(function() tween(bhBtn,{BackgroundColor3=Color3.fromRGB(30,110,35)},0.15); tween(bhBtnStroke,{Thickness=3},0.15) end)
    bhBtn.MouseLeave:Connect(function() tween(bhBtn,{BackgroundColor3=Color3.fromRGB(20,80,25)},0.15); tween(bhBtnStroke,{Thickness=2},0.15) end)



    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- LABORATÃ“RIO BRAINROT
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

    -- Card visual na aba GAMES
    local lbCard = Instance.new("Frame", gamesTab.page)
    lbCard.Size=UDim2.new(1,-6,0,80); lbCard.BackgroundColor3=Color3.fromRGB(8,20,8)
    lbCard.LayoutOrder=3; corner(lbCard,10); stroke(lbCard,Color3.fromRGB(0,200,80),1.5)
    grad(lbCard,Color3.fromRGB(12,32,12),Color3.fromRGB(6,16,6),90)
    local lbIco=Instance.new("Frame",lbCard); lbIco.Size=UDim2.new(0,72,0,72); lbIco.Position=UDim2.new(0,4,0,4)
    lbIco.BackgroundColor3=Color3.fromRGB(10,38,14); corner(lbIco,10); stroke(lbIco,Color3.fromRGB(0,170,55),1)
    lbl(lbIco,"ðŸ§ª",UDim2.new(1,0,0,44),UDim2.new(0,0,0,0),30,Color3.fromRGB(0,255,100))
    lbl(lbIco,"ðŸ§ ",UDim2.new(1,0,0,30),UDim2.new(0,0,0,42),18,Color3.fromRGB(200,100,255))
    lbl(lbCard,"ðŸ§ª LABORATÃ“RIO BRAINROT",UDim2.new(1,-88,0,22),UDim2.new(0,82,0,5),11,Color3.fromRGB(0,255,80),Enum.TextXAlignment.Left).Font=Enum.Font.GothamBold
    local lbSub=lbl(lbCard,"âœ… Main | Buy ALL | Farm | Credits\nâš¡ By AVEN  |  AvenHub V22",UDim2.new(1,-88,0,38),UDim2.new(0,82,0,26),9,Color3.fromRGB(100,200,120),Enum.TextXAlignment.Left)
    lbSub.TextWrapped=true

    -- BotÃ£o abrir
    local lbOpenBtn=Instance.new("TextButton",gamesTab.page)
    lbOpenBtn.Size=UDim2.new(1,-6,0,42); lbOpenBtn.BackgroundColor3=Color3.fromRGB(10,55,17)
    lbOpenBtn.Text="ðŸ§ª  ABRIR LAB BRAINROT HUB"; lbOpenBtn.TextSize=13; lbOpenBtn.Font=Enum.Font.GothamBold
    lbOpenBtn.TextColor3=Color3.fromRGB(0,255,100); lbOpenBtn.AutoButtonColor=false; lbOpenBtn.LayoutOrder=4
    corner(lbOpenBtn,10); grad(lbOpenBtn,Color3.fromRGB(14,72,20),Color3.fromRGB(8,36,12),90)
    local lbOS=Instance.new("UIStroke",lbOpenBtn); lbOS.Color=Color3.fromRGB(0,200,80); lbOS.Thickness=2
    lbOpenBtn.MouseEnter:Connect(function() tween(lbOpenBtn,{BackgroundColor3=Color3.fromRGB(14,85,24)},0.15) end)
    lbOpenBtn.MouseLeave:Connect(function() tween(lbOpenBtn,{BackgroundColor3=Color3.fromRGB(10,55,17)},0.15) end)

    -- â•â• PAINEL PRINCIPAL â•â•
    local LB_W,LB_H=600,510
    local lbPanel=Instance.new("Frame",gui)
    lbPanel.Size=UDim2.new(0,LB_W,0,LB_H); lbPanel.Position=UDim2.new(0.5,-LB_W/2,0.5,-LB_H/2)
    lbPanel.BackgroundColor3=Color3.fromRGB(38,38,38); lbPanel.Active=true; lbPanel.Draggable=true
    lbPanel.Visible=false; lbPanel.ZIndex=50; corner(lbPanel,12)
    stroke(lbPanel,Color3.fromRGB(0,200,80),2)
    -- RGB border animado
    local lbStr=Instance.new("UIStroke",lbPanel); lbStr.Thickness=2.5; lbStr.Color=Color3.fromRGB(0,200,80)
    task.spawn(function()
        local h=80
        while lbPanel and lbPanel.Parent do h=(h+1)%360; lbStr.Color=Color3.fromHSV(h/360,1,1); task.wait(0.05) end
    end)

    -- Topbar escura igual Phantom Hub
    local lbTop=Instance.new("Frame",lbPanel)
    lbTop.Size=UDim2.new(1,0,0,40); lbTop.BackgroundColor3=Color3.fromRGB(28,28,28); lbTop.ZIndex=51; corner(lbTop,12)
    local lbTopFix=Instance.new("Frame",lbTop); lbTopFix.Size=UDim2.new(1,0,0.5,0); lbTopFix.Position=UDim2.new(0,0,0.5,0)
    lbTopFix.BackgroundColor3=Color3.fromRGB(28,28,28); lbTopFix.BorderSizePixel=0; lbTopFix.ZIndex=51
    lbl(lbTop,"ðŸ§ª LABORATÃ“RIO BRAINROT  |  âš¡ By AVEN",UDim2.new(1,-44,1,0),UDim2.new(0,12,0,0),12,Color3.fromRGB(255,255,255),Enum.TextXAlignment.Left).ZIndex=52
    local lbClsBtn=btn(lbTop,"X",UDim2.new(0,28,0,28),UDim2.new(1,-36,0.5,-14),Color3.fromRGB(180,30,30),12); lbClsBtn.ZIndex=53

    local lbOpen=false
    local function lbClose()
        tween(lbPanel,{Size=UDim2.new(0,LB_W,0,0),Position=UDim2.new(0.5,-LB_W/2,0.5,0)},0.2)
        task.wait(0.22); lbPanel.Visible=false
        lbPanel.Size=UDim2.new(0,LB_W,0,LB_H); lbPanel.Position=UDim2.new(0.5,-LB_W/2,0.5,-LB_H/2); lbOpen=false
    end
    lbClsBtn.MouseButton1Click:Connect(lbClose)

    -- Mini botÃ£o arrastÃ¡vel
    local lbMini=Instance.new("TextButton",gui)
    lbMini.Size=UDim2.new(0,54,0,54); lbMini.Position=UDim2.new(1,-145,0.5,-27)
    lbMini.Text=""; lbMini.BackgroundColor3=Color3.fromRGB(8,40,12)
    lbMini.Active=true; lbMini.Draggable=true; lbMini.ZIndex=60; corner(lbMini,27)
    local lbMS=Instance.new("UIStroke",lbMini); lbMS.Thickness=2; lbMS.Color=Color3.fromRGB(0,200,80)
    task.spawn(function()
        local h=80
        while lbMini and lbMini.Parent do h=(h+1)%360; lbMS.Color=Color3.fromHSV(h/360,1,1); task.wait(0.06) end
    end)
    lbl(lbMini,"ðŸ§ª",UDim2.new(1,0,0,32),UDim2.new(0,0,0,3),20,Color3.fromRGB(0,255,100)).ZIndex=61
    lbl(lbMini,"LAB",UDim2.new(1,0,0,14),UDim2.new(0,0,1,-14),7,Color3.fromRGB(0,200,80)).ZIndex=61
    lbMini.MouseButton1Click:Connect(function()
        if lbOpen then lbClose(); tween(lbMini,{BackgroundColor3=Color3.fromRGB(8,40,12)},0.15)
        else
            lbPanel.Visible=true; lbPanel.Size=UDim2.new(0,0,0,0); lbPanel.Position=UDim2.new(0.5,0,0.5,0)
            tween(lbPanel,{Size=UDim2.new(0,LB_W,0,LB_H),Position=UDim2.new(0.5,-LB_W/2,0.5,-LB_H/2)},0.3,Enum.EasingStyle.Back,Enum.EasingDirection.Out)
            lbOpen=true; tween(lbMini,{BackgroundColor3=Color3.fromRGB(14,72,20)},0.15)
        end
    end)

    -- â•â• SIDEBAR igual Phantom Hub â•â•
    -- Sidebar escura com botÃµes grandes (Main / Buy ALL / Farm / Credits)
    local lbSide=Instance.new("Frame",lbPanel)
    lbSide.Size=UDim2.new(0,145,1,-40); lbSide.Position=UDim2.new(0,0,0,40)
    lbSide.BackgroundColor3=Color3.fromRGB(45,45,45); lbSide.BorderSizePixel=0; lbSide.ZIndex=51
    local lbSL=Instance.new("UIListLayout",lbSide)
    lbSL.Padding=UDim.new(0,6); lbSL.HorizontalAlignment=Enum.HorizontalAlignment.Center
    local lbSP=Instance.new("UIPadding",lbSide)
    lbSP.PaddingTop=UDim.new(0,10); lbSP.PaddingLeft=UDim.new(0,8); lbSP.PaddingRight=UDim.new(0,8)

    -- Content area
    local lbCont=Instance.new("Frame",lbPanel)
    lbCont.Size=UDim2.new(1,-151,1,-46); lbCont.Position=UDim2.new(0,148,0,43)
    lbCont.BackgroundColor3=Color3.fromRGB(48,48,48); lbCont.ZIndex=51; corner(lbCont,8)

    local lbTabs={}; local lbAct=nil

    -- Cria aba (botÃ£o sidebar + pÃ¡gina scrollÃ¡vel)
    local function newLbTab(nm)
        local tb=Instance.new("TextButton",lbSide)
        tb.Size=UDim2.new(1,0,0,50); tb.Text=nm; tb.TextSize=14
        tb.Font=Enum.Font.GothamBold; tb.TextColor3=Color3.fromRGB(200,200,200)
        tb.BackgroundColor3=Color3.fromRGB(65,65,65); tb.AutoButtonColor=false; tb.ZIndex=52; corner(tb,8)

        local pg=Instance.new("ScrollingFrame",lbCont)
        pg.Size=UDim2.new(1,0,1,0); pg.BackgroundTransparency=1
        pg.ScrollBarThickness=4; pg.ScrollBarImageColor3=Color3.fromRGB(0,200,80)
        pg.CanvasSize=UDim2.new(0,0,0,0); pg.AutomaticCanvasSize=Enum.AutomaticSize.Y
        pg.Visible=false; pg.ZIndex=52; pg.BorderSizePixel=0
        local pl=Instance.new("UIListLayout",pg); pl.Padding=UDim.new(0,4); pl.SortOrder=Enum.SortOrder.LayoutOrder
        local pp=Instance.new("UIPadding",pg)
        pp.PaddingTop=UDim.new(0,8); pp.PaddingLeft=UDim.new(0,10); pp.PaddingRight=UDim.new(0,10); pp.PaddingBottom=UDim.new(0,10)

        local t={btn=tb,page=pg}; table.insert(lbTabs,t)
        tb.MouseButton1Click:Connect(function()
            if lbAct==t then return end
            if lbAct then
                lbAct.page.Visible=false
                tween(lbAct.btn,{BackgroundColor3=Color3.fromRGB(65,65,65),TextColor3=Color3.fromRGB(200,200,200)},0.15)
            end
            lbAct=t; pg.Visible=true
            tween(tb,{BackgroundColor3=Color3.fromRGB(14,60,20),TextColor3=Color3.fromRGB(255,255,255)},0.15)
        end)
        return t
    end

    -- Toggle row idÃªntico ao Phantom Hub (fundo cinza, label esquerda, toggle direita)
    local function lbTogRow(page,labelTxt,lo,cb)
        local row=Instance.new("Frame",page)
        row.Size=UDim2.new(1,0,0,52); row.BackgroundColor3=Color3.fromRGB(62,62,62)
        row.LayoutOrder=lo or 99; corner(row,8)

        -- Label
        local lb2=lbl(row,labelTxt,UDim2.new(1,-68,1,0),UDim2.new(0,12,0,0),12,Color3.fromRGB(210,210,210),Enum.TextXAlignment.Left)
        lb2.TextWrapped=true

        -- Toggle switch (fundo + cÃ­rculo)
        local tBg=Instance.new("Frame",row)
        tBg.Size=UDim2.new(0,50,0,26); tBg.Position=UDim2.new(1,-62,0.5,-13)
        tBg.BackgroundColor3=Color3.fromRGB(90,90,90); corner(tBg,13); tBg.ZIndex=53

        local tCirc=Instance.new("Frame",tBg)
        tCirc.Size=UDim2.new(0,20,0,20); tCirc.Position=UDim2.new(0,3,0.5,-10)
        tCirc.BackgroundColor3=Color3.fromRGB(220,220,220); corner(tCirc,10); tCirc.ZIndex=54

        local state=false
        -- BotÃ£o invisÃ­vel sobre a row inteira
        local clickBtn=Instance.new("TextButton",row)
        clickBtn.Size=UDim2.new(1,0,1,0); clickBtn.BackgroundTransparency=1
        clickBtn.Text=""; clickBtn.ZIndex=55
        clickBtn.MouseButton1Click:Connect(function()
            state=not state
            if state then
                tween(tBg,{BackgroundColor3=Color3.fromRGB(0,185,65)},0.18)
                tween(tCirc,{Position=UDim2.new(0,27,0.5,-10)},0.18)
            else
                tween(tBg,{BackgroundColor3=Color3.fromRGB(90,90,90)},0.18)
                tween(tCirc,{Position=UDim2.new(0,3,0.5,-10)},0.18)
            end
            if cb then cb(state) end
        end)
        return row,tBg,tCirc,function() return state end
    end

    -- BotÃ£o simples (para Parar Tudo, lista de brainrots, etc)
    local function lbBtn3(page,txt,col,lo,cb)
        local b=Instance.new("TextButton",page)
        b.Size=UDim2.new(1,0,0,42); b.BackgroundColor3=col or Color3.fromRGB(62,62,62)
        b.Text=txt; b.TextSize=12; b.Font=Enum.Font.GothamBold
        b.TextColor3=Color3.new(1,1,1); b.AutoButtonColor=false; b.LayoutOrder=lo or 99; b.ZIndex=53
        corner(b,8)
        b.MouseEnter:Connect(function() tween(b,{BackgroundTransparency=0.2},0.1) end)
        b.MouseLeave:Connect(function() tween(b,{BackgroundTransparency=0},0.1) end)
        if cb then b.MouseButton1Click:Connect(cb) end
        return b
    end

    -- Separador com texto
    local function lbSep(page,txt,lo)
        local f=Instance.new("Frame",page); f.Size=UDim2.new(1,0,0,22); f.BackgroundTransparency=1; f.LayoutOrder=lo or 99
        lbl(f,txt,UDim2.new(1,0,1,0),UDim2.new(0,0,0,0),9,Color3.fromRGB(0,200,80),Enum.TextXAlignment.Left)
        local line=Instance.new("Frame",f); line.Size=UDim2.new(1,0,0,1); line.Position=UDim2.new(0,0,1,-1)
        line.BackgroundColor3=Color3.fromRGB(0,120,40); line.BorderSizePixel=0; return f
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- ABA: MAIN  (igual Phantom Hub)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local lbMainTab=newLbTab("Main")
    do
        -- Controles de estado
        local rebOn=false; local buyOn=false; local collOn=false; local espOn=false
        local lowOn=false; local instOn=false; local ragOn=false; local asOn=false; local afOn=false

        -- Todas as toggle rows para poder resetar depois
        local toggleBgs={}

        local function addTogBg(tBg,tCirc) table.insert(toggleBgs,{bg=tBg,circ=tCirc}) end

        lbSep(lbMainTab.page,"â”€â”€â”€ AUTO FARM â”€â”€â”€",0)

        -- AUTO REBIRTH
        local rebCnt=0
        local r1,b1,c1=lbTogRow(lbMainTab.page,"Auto Rebirth",1,function(on)
            rebOn=on
            if on then notify("Lab","ðŸ”„ Auto Rebirth ON!",2)
                task.spawn(function()
                    while rebOn do
                        pcall(function()
                            for _,v in pairs(player.PlayerGui:GetDescendants()) do
                                if (v:IsA("TextButton") or v:IsA("ImageButton")) and v.Visible then
                                    local t=string.lower(v.Text or "")
                                    if string.find(t,"rebirth") or string.find(t,"prestige") then
                                        v.MouseButton1Click:Fire(); rebCnt=rebCnt+1
                                    end
                                end
                            end
                        end); task.wait(3)
                    end
                end)
            end
        end); addTogBg(b1,c1)

        -- AUTO BUY BRAINROTS
        local buyCnt=0
        local r2,b2,c2=lbTogRow(lbMainTab.page,"Auto Buy Brainrots",2,function(on)
            buyOn=on
            if on then notify("Lab","ðŸ§  Auto Buy Brainrots ON!",2)
                task.spawn(function()
                    while buyOn do
                        pcall(function()
                            for _,v in pairs(player.PlayerGui:GetDescendants()) do
                                if (v:IsA("TextButton") or v:IsA("ImageButton")) and v.Visible then
                                    local t=string.lower(v.Text or "")
                                    if string.find(t,"buy") or string.find(t,"purchase") or string.find(t,"comprar") then
                                        v.MouseButton1Click:Fire(); buyCnt=buyCnt+1
                                    end
                                end
                            end
                        end); task.wait(1.5)
                    end
                end)
            end
        end); addTogBg(b2,c2)

        -- AUTO COLLECT MONEY
        local collCnt=0
        local r3,b3,c3=lbTogRow(lbMainTab.page,"Auto Collect Money",3,function(on)
            collOn=on
            if on then notify("Lab","ðŸ’° Auto Collect Money ON!",2)
                task.spawn(function()
                    while collOn do
                        pcall(function()
                            local char=player.Character; if not char then return end
                            local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
                            for _,obj in pairs(workspace:GetDescendants()) do
                                if obj:IsA("BasePart") or obj:IsA("MeshPart") then
                                    local n=string.lower(obj.Name)
                                    if string.find(n,"money") or string.find(n,"cash") or string.find(n,"coin") or string.find(n,"pickup") then
                                        if (hrp.Position-obj.Position).Magnitude<120 then
                                            local old=hrp.CFrame
                                            hrp.CFrame=CFrame.new(obj.Position+Vector3.new(0,2,0)); task.wait(0.05); hrp.CFrame=old
                                            collCnt=collCnt+1
                                        end
                                    end
                                end
                            end
                        end); task.wait(1.2)
                    end
                end)
            end
        end); addTogBg(b3,c3)

        lbSep(lbMainTab.page,"â”€â”€â”€ VISUAL / PROTEÃ‡ÃƒO â”€â”€â”€",4)

        -- PLAYER ESP RED AURA
        local r4,b4,c4=lbTogRow(lbMainTab.page,"Player ESP (Red Aura)",5,function(on)
            espOn=on
            if on then notify("Lab","ðŸ‘ï¸ Player ESP ON!",2)
                for _,p in pairs(Players:GetPlayers()) do
                    if p~=player and p.Character then
                        local hrp=p.Character:FindFirstChild("HumanoidRootPart")
                        if hrp and not hrp:FindFirstChild("LbESP") then
                            local bb=Instance.new("BillboardGui",hrp); bb.Name="LbESP"
                            bb.Size=UDim2.new(0,90,0,32); bb.StudsOffset=Vector3.new(0,4,0); bb.AlwaysOnTop=true
                            local nl=Instance.new("TextLabel",bb); nl.Size=UDim2.new(1,0,0.6,0); nl.BackgroundTransparency=1
                            nl.Text=p.Name; nl.TextScaled=true; nl.Font=Enum.Font.GothamBold
                            nl.TextColor3=Color3.fromRGB(255,60,60); nl.TextStrokeTransparency=0
                            local dl=Instance.new("TextLabel",bb); dl.Size=UDim2.new(1,0,0.4,0); dl.Position=UDim2.new(0,0,0.6,0)
                            dl.BackgroundTransparency=1; dl.TextScaled=true; dl.Font=Enum.Font.Gotham
                            dl.TextColor3=Color3.fromRGB(255,180,180); dl.TextStrokeTransparency=0
                            pcall(function()
                                local hl=Instance.new("SelectionBox"); hl.Name="LbHL"
                                hl.Adornee=p.Character; hl.Color3=Color3.fromRGB(255,0,0)
                                hl.LineThickness=0.04; hl.SurfaceTransparency=0.75
                                hl.SurfaceColor3=Color3.fromRGB(255,50,50); hl.Parent=p.Character
                            end)
                            local ch=hrp
                            task.spawn(function()
                                while espOn and bb and bb.Parent do
                                    pcall(function()
                                        local mh=player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                                        if mh and ch and ch.Parent then
                                            dl.Text=string.format("%.0fm",(mh.Position-ch.Position).Magnitude)
                                        end
                                    end); task.wait(0.3)
                                end
                            end)
                        end
                    end
                end
            else notify("Lab","ðŸ”´ ESP OFF",1.5)
                for _,p in pairs(Players:GetPlayers()) do
                    if p.Character then
                        for _,v in pairs(p.Character:GetDescendants()) do
                            if v.Name=="LbESP" or v.Name=="LbHL" then v:Destroy() end
                        end
                    end
                end
            end
        end); addTogBg(b4,c4)

        -- LOW MODE
        local r5,b5,c5=lbTogRow(lbMainTab.page,"Low Mode",6,function(on)
            lowOn=on
            if on then
                pcall(function() game:GetService("Lighting").GlobalShadows=false; settings().Rendering.QualityLevel=Enum.QualityLevel.Level01 end)
                task.spawn(function()
                    while lowOn do
                        pcall(function()
                            for _,v in pairs(workspace:GetDescendants()) do
                                if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") then v.Enabled=false end
                            end
                        end); task.wait(5)
                    end
                end)
                notify("Lab","ðŸ–¥ï¸ Low Mode ON!",2)
            else
                pcall(function() game:GetService("Lighting").GlobalShadows=true; settings().Rendering.QualityLevel=Enum.QualityLevel.Automatic end)
                notify("Lab","ðŸ”´ Low Mode OFF",1.5)
            end
        end); addTogBg(b5,c5)

        -- INSTANT COLLECT BRAINROTS
        local instCnt=0
        local r6,b6,c6=lbTogRow(lbMainTab.page,"Instant Collect Brainrots",7,function(on)
            instOn=on
            if on then notify("Lab","âš¡ Instant Collect ON!",2)
                task.spawn(function()
                    while instOn do
                        pcall(function()
                            local char=player.Character; if not char then return end
                            local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
                            for _,obj in pairs(workspace:GetDescendants()) do
                                local n=string.lower(obj.Name)
                                if string.find(n,"brainrot") or string.find(n,"ingredient") or string.find(n,"bandito") or string.find(n,"tralalero") or string.find(n,"tung") then
                                    local pos
                                    if obj:IsA("BasePart") then pos=obj.Position
                                    elseif obj:IsA("Model") and obj.PrimaryPart then pos=obj.PrimaryPart.Position end
                                    if pos and (hrp.Position-pos).Magnitude<200 then
                                        local old=hrp.CFrame; hrp.CFrame=CFrame.new(pos+Vector3.new(0,2,0)); task.wait(0.05); hrp.CFrame=old
                                        instCnt=instCnt+1
                                    end
                                end
                            end
                        end); task.wait(0.8)
                    end
                end)
            end
        end); addTogBg(b6,c6)

        -- ANTI RAGDOLL
        local r7,b7,c7=lbTogRow(lbMainTab.page,"Anti Ragdoll",8,function(on)
            ragOn=on
            if on then notify("Lab","ðŸ›¡ï¸ Anti Ragdoll ON!",2)
                task.spawn(function()
                    while ragOn do
                        pcall(function()
                            local char=player.Character; if not char then return end
                            local hum=char:FindFirstChildOfClass("Humanoid"); if not hum then return end
                            for _,v in pairs(char:GetDescendants()) do
                                if v:IsA("BallSocketConstraint") or v:IsA("NoCollisionConstraint") then v.Enabled=false end
                            end
                            hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll,false)
                            hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown,false)
                            if hum:GetState()==Enum.HumanoidStateType.Ragdoll or hum:GetState()==Enum.HumanoidStateType.FallingDown then
                                hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                            end
                        end); task.wait(0.3)
                    end
                end)
            end
        end); addTogBg(b7,c7)

        lbSep(lbMainTab.page,"â”€â”€â”€ PROTEÃ‡ÃƒO â”€â”€â”€",9)

        -- ANTI SLAP
        local r8,b8,c8=lbTogRow(lbMainTab.page,"Anti Slap",10,function(on)
            asOn=on
            if on then notify("Lab","ðŸ›¡ï¸ Anti Slap ON!",3)
                task.spawn(function()
                    while asOn do
                        pcall(function()
                            local char=player.Character; if not char then return end
                            local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
                            if hrp.AssemblyLinearVelocity.Magnitude>85 then
                                hrp.AssemblyLinearVelocity=Vector3.new(0,0,0); hrp.AssemblyAngularVelocity=Vector3.new(0,0,0)
                            end
                            for _,p in pairs(Players:GetPlayers()) do
                                if p~=player and p.Character then
                                    local ph=p.Character:FindFirstChild("HumanoidRootPart")
                                    if ph and (hrp.Position-ph.Position).Magnitude<7 then
                                        for _,t in pairs(p.Character:GetChildren()) do
                                            if t:IsA("Tool") then
                                                local tn=string.lower(t.Name)
                                                if string.find(tn,"slap") or string.find(tn,"bat") or string.find(tn,"hit") or string.find(tn,"punch") then
                                                    hrp.CFrame=hrp.CFrame*CFrame.new(0,5,-12)
                                                    notify("Anti Slap","âš ï¸ Fugiu de "..p.Name.."!",2)
                                                end
                                            end
                                        end
                                    end
                                end
                            end
                        end); task.wait(0.15)
                    end
                end)
            else notify("Lab","ðŸ”´ Anti Slap OFF",2) end
        end); addTogBg(b8,c8)

        -- ANTI FLING
        local r9,b9,c9=lbTogRow(lbMainTab.page,"Anti Fling",11,function(on)
            afOn=on
            if on then notify("Lab","ðŸŒ€ Anti Fling ON!",2)
                task.spawn(function()
                    while afOn do
                        pcall(function()
                            local hrp=player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                            if hrp and hrp.AssemblyLinearVelocity.Magnitude>100 then
                                hrp.AssemblyLinearVelocity=Vector3.new(0,0,0); hrp.AssemblyAngularVelocity=Vector3.new(0,0,0)
                            end
                        end); task.wait(0.1)
                    end
                end)
            end
        end); addTogBg(b9,c9)

        lbSep(lbMainTab.page,"â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€",20)

        -- PARAR TUDO
        lbBtn3(lbMainTab.page,"ðŸ›‘  PARAR TODOS OS SISTEMAS",Color3.fromRGB(140,22,22),21,function()
            rebOn=false; buyOn=false; collOn=false; espOn=false
            lowOn=false; instOn=false; ragOn=false; asOn=false; afOn=false
            -- Reset visual de todos os toggles
            for _,t in ipairs(toggleBgs) do
                tween(t.bg,{BackgroundColor3=Color3.fromRGB(90,90,90)},0.15)
                tween(t.circ,{Position=UDim2.new(0,3,0.5,-10)},0.15)
            end
            -- Limpa ESP
            for _,p in pairs(Players:GetPlayers()) do
                if p.Character then
                    for _,v in pairs(p.Character:GetDescendants()) do
                        if v.Name=="LbESP" or v.Name=="LbHL" then v:Destroy() end
                    end
                end
            end
            pcall(function() game:GetService("Lighting").GlobalShadows=true; settings().Rendering.QualityLevel=Enum.QualityLevel.Automatic end)
            notify("Lab","ðŸ›‘ Tudo parado!",2)
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- ABA: BUY ALL
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local lbBuyTab=newLbTab("Buy ALL")
    do
        lbSep(lbBuyTab.page,"â”€â”€â”€ COMPRAR TUDO AUTOMÃTICO â”€â”€â”€",0)

        local baCnt=0; local baOn=false; local baBgs={}
        local ra,ba,ca=lbTogRow(lbBuyTab.page,"Buy ALL Brainrots",1,function(on)
            baOn=on
            if on then notify("Lab","ðŸ›’ Buy ALL ON!",2)
                task.spawn(function()
                    while baOn do
                        pcall(function()
                            for _,v in pairs(player.PlayerGui:GetDescendants()) do
                                if (v:IsA("TextButton") or v:IsA("ImageButton")) and v.Visible then
                                    local t=string.lower(v.Text or "")
                                    if string.find(t,"buy") or string.find(t,"purchase") or string.find(t,"$") then
                                        v.MouseButton1Click:Fire(); baCnt=baCnt+1; task.wait(0.05)
                                    end
                                end
                            end
                        end); task.wait(1.5)
                    end
                end)
            end
        end)
        table.insert(baBgs,{bg=ba,circ=ca})

        lbSep(lbBuyTab.page,"â”€â”€â”€ COMPRAR BRAINROT ESPECÃFICO â”€â”€â”€",2)

        local allBr={
            "Bandito Bobrito","Tralalero Tralala","Bombardino Crocodilo",
            "Ballerina Cappuccino","Tung Tung Sahur","Cappuccino Assassino",
            "Bombombini Gusini","Trippi Troppi","La Vacca Saturno",
            "Brr Brr Patapim","Lirili Larila","Glorbo Fruttodrillo",
            "Frigo Camelo","Ciucciuteiu","Bobrito Bandito",
        }
        for i,nm in ipairs(allBr) do
            lbBtn3(lbBuyTab.page,"ðŸ§   "..nm,Color3.fromRGB(55,55,55),i+2,function()
                pcall(function()
                    for _,v in pairs(player.PlayerGui:GetDescendants()) do
                        if (v:IsA("TextButton") or v:IsA("ImageButton")) and v.Visible then
                            local t=string.lower(v.Text or "")
                            if string.find(t,string.lower(nm):sub(1,6)) then v.MouseButton1Click:Fire() end
                        end
                    end
                end); notify("Buy","ðŸ§  "..nm,1.5)
            end)
        end
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- ABA: FARM
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local lbFarmTab=newLbTab("Farm")
    do
        lbSep(lbFarmTab.page,"â”€â”€â”€ AUTO FARM INGREDIENTES â”€â”€â”€",0)
        local farmBgs={}

        local function mkFarm(nm,kws,lo)
            local fOn=false
            local rf,bf,cf=lbTogRow(lbFarmTab.page,nm,lo,function(state)
                fOn=state
                if state then notify("Farm","ðŸŒ± "..nm.." ON!",1.5)
                    task.spawn(function()
                        while fOn do
                            pcall(function()
                                local char=player.Character; if not char then return end
                                local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
                                -- Clica na UI
                                for _,v in pairs(player.PlayerGui:GetDescendants()) do
                                    if (v:IsA("TextButton") or v:IsA("ImageButton")) and v.Visible then
                                        local t=string.lower(v.Text or "")
                                        for _,kw in ipairs(kws) do if string.find(t,kw) then v.MouseButton1Click:Fire() end end
                                    end
                                end
                                -- TP para objeto no workspace
                                for _,obj in pairs(workspace:GetDescendants()) do
                                    local n=string.lower(obj.Name)
                                    for _,kw in ipairs(kws) do
                                        if string.find(n,kw) then
                                            local pos
                                            if obj:IsA("BasePart") then pos=obj.Position
                                            elseif obj:IsA("Model") and obj.PrimaryPart then pos=obj.PrimaryPart.Position end
                                            if pos and (hrp.Position-pos).Magnitude<250 then
                                                local old=hrp.CFrame; hrp.CFrame=CFrame.new(pos+Vector3.new(0,2,0)); task.wait(0.05); hrp.CFrame=old
                                            end; break
                                        end
                                    end
                                end
                            end); task.wait(0.8)
                        end
                    end)
                end
            end)
            table.insert(farmBgs,{bg=bf,circ=cf,ref=function() fOn=false end})
        end

        -- Exatamente iguais Ã s fotos
        mkFarm("Auto Ice Emblem",{"iceemblem","ice emblem","emblem"},1)
        mkFarm("Auto Victrola",{"victrola"},2)
        mkFarm("Auto Star",{"star","estrela"},3)
        mkFarm("Auto Flower",{"flower","flor"},4)
        mkFarm("Auto Phone",{"phone","telefone"},5)
        mkFarm("Auto Gem",{"gem","gema"},6)
        mkFarm("Auto Orb",{"orb"},7)
        mkFarm("Auto Coin",{"coin","moeda"},8)

        lbSep(lbFarmTab.page,"â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€",20)
        lbBtn3(lbFarmTab.page,"ðŸ›‘  Parar TODOS os Farms",Color3.fromRGB(140,22,22),21,function()
            for _,t in ipairs(farmBgs) do
                if t.ref then t.ref() end
                tween(t.bg,{BackgroundColor3=Color3.fromRGB(90,90,90)},0.15)
                tween(t.circ,{Position=UDim2.new(0,3,0.5,-10)},0.15)
            end
            notify("Farm","ðŸ›‘ Todos os farms parados!",2)
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- ABA: CREDITS
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local lbCredTab=newLbTab("Credits")
    do
        -- Card Aven dourado
        local cc=Instance.new("Frame",lbCredTab.page)
        cc.Size=UDim2.new(1,0,0,88); cc.BackgroundColor3=C.GOLD; cc.LayoutOrder=0
        corner(cc,12); stroke(cc,Color3.fromRGB(255,215,0),3); grad(cc,Color3.fromRGB(255,210,0),Color3.fromRGB(255,168,0),45)
        lbl(cc,"ðŸ‘‘",UDim2.new(0,60,0,60),UDim2.new(0,12,0,14),44,C.WHITE)
        lbl(cc,"AVEN",UDim2.new(1,-85,0,32),UDim2.new(0,80,0,10),22,Color3.fromRGB(0,0,0),Enum.TextXAlignment.Left).Font=Enum.Font.GothamBold
        lbl(cc,"OWNER  |  AvenHub V22",UDim2.new(1,-85,0,18),UDim2.new(0,80,0,42),11,Color3.fromRGB(55,35,0),Enum.TextXAlignment.Left)
        lbl(cc,"ðŸ§ª Lab Brainrot Edition",UDim2.new(1,-85,0,16),UDim2.new(0,80,0,62),10,Color3.fromRGB(75,50,0),Enum.TextXAlignment.Left)

        lbSep(lbCredTab.page,"â”€â”€â”€ SOBRE O HUB â”€â”€â”€",1)

        local infos={
            "ðŸ§ª LaboratÃ³rio Brainrot Hub",
            "âš¡ Estilo: Phantom Hub",
            "ðŸ‘‘ By AVEN  |  AvenHub V22",
            "",
            "âœ… Auto Rebirth",
            "âœ… Auto Buy Brainrots",
            "âœ… Auto Collect Money",
            "âœ… Player ESP (Red Aura)",
            "âœ… Low Mode (FPS+)",
            "âœ… Instant Collect Brainrots",
            "âœ… Anti Ragdoll",
            "âœ… Anti Slap  +  Anti Fling",
            "âœ… Buy ALL Brainrots (15+)",
            "âœ… Auto Ice Emblem",
            "âœ… Auto Victrola",
            "âœ… Auto Star",
            "âœ… Auto Flower",
            "âœ… Auto Phone",
            "âœ… Auto Gem / Orb / Coin",
        }
        for i,txt in ipairs(infos) do
            local f=Instance.new("Frame",lbCredTab.page)
            f.Size=UDim2.new(1,0,0,txt=="" and 6 or 22); f.BackgroundTransparency=1; f.LayoutOrder=i+1
            if txt~="" then
                local col=string.find(txt,"âœ…") and Color3.fromRGB(0,200,80) or Color3.fromRGB(210,210,210)
                lbl(f,txt,UDim2.new(1,0,1,0),UDim2.new(0,4,0,0),11,col,Enum.TextXAlignment.Left)
            end
        end
    end

    -- Ativa primeira aba ao abrir
    task.defer(function()
        task.wait(0.05)
        if #lbTabs>0 then lbTabs[1].btn.MouseButton1Click:Fire() end
    end)

    -- BotÃ£o GAMES â†’ abre painel
    lbOpenBtn.MouseButton1Click:Connect(function()
        lbPanel.Visible=true; lbPanel.Size=UDim2.new(0,0,0,0); lbPanel.Position=UDim2.new(0.5,0,0.5,0)
        tween(lbPanel,{Size=UDim2.new(0,LB_W,0,LB_H),Position=UDim2.new(0.5,-LB_W/2,0.5,-LB_H/2)},0.3,Enum.EasingStyle.Back,Enum.EasingDirection.Out)
        lbOpen=true; tween(lbMini,{BackgroundColor3=Color3.fromRGB(14,72,20)},0.15)
        task.wait(0.05)
        if #lbTabs>0 then lbTabs[1].btn.MouseButton1Click:Fire() end
    end)

    -- PAINEL BROOKHAVEN+ (janela separada grande)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local BH_W, BH_H = 650, 540
    local bhPanel = Instance.new("Frame", gui)
    bhPanel.Size = UDim2.new(0,BH_W,0,BH_H)
    bhPanel.Position = UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2)
    bhPanel.BackgroundColor3 = Color3.fromRGB(12,12,18)
    bhPanel.Active = true; bhPanel.Draggable = true
    bhPanel.Visible = false; bhPanel.ZIndex = 50
    corner(bhPanel, 14)
    local bhPanelStroke = Instance.new("UIStroke", bhPanel)
    bhPanelStroke.Thickness = 2.5; bhPanelStroke.Color = Color3.fromRGB(120,70,255)
    task.spawn(function()
        local h = 200
        while bhPanel and bhPanel.Parent do
            h = (h+1)%360; bhPanelStroke.Color = Color3.fromHSV(h/360,1,1); task.wait(0.05)
        end
    end)

    -- TopBar do painel BH
    local bhTopBar = Instance.new("Frame", bhPanel)
    bhTopBar.Size = UDim2.new(1,0,0,42); bhTopBar.BackgroundColor3 = Color3.fromRGB(18,10,30)
    bhTopBar.ZIndex = 51; corner(bhTopBar, 14)
    local bhTopFix = Instance.new("Frame", bhTopBar)
    bhTopFix.Size = UDim2.new(1,0,0.5,0); bhTopFix.Position = UDim2.new(0,0,0.5,0)
    bhTopFix.BackgroundColor3 = Color3.fromRGB(18,10,30); bhTopFix.BorderSizePixel = 0; bhTopFix.ZIndex = 51

    -- Thumbnail mini no topbar
    local bhTopImg = Instance.new("ImageLabel", bhTopBar)
    bhTopImg.Size = UDim2.new(0,32,0,32); bhTopImg.Position = UDim2.new(0,8,0.5,-16)
    bhTopImg.Image = "rbxassetid://5672420098"; bhTopImg.BackgroundTransparency = 1
    bhTopImg.ZIndex = 52; corner(bhTopImg, 6)

    lbl(bhTopBar, "ðŸ¡ BROOKHAVEN+ HUB",
        UDim2.new(1,-130,1,0), UDim2.new(0,46,0,0),
        13, Color3.fromRGB(200,150,255), Enum.TextXAlignment.Left).ZIndex = 52

    -- BotÃ£o FECHAR
    local bhClose = btn(bhTopBar, "âœ•", UDim2.new(0,26,0,26), UDim2.new(1,-34,0.5,-13), C.RED, 12)
    bhClose.ZIndex = 53
    bhClose.MouseButton1Click:Connect(function()
        tween(bhPanel,{Size=UDim2.new(0,BH_W,0,0),Position=UDim2.new(0.5,-BH_W/2,0.5,0)},0.2)
        task.wait(0.22); bhPanel.Visible = false
        bhPanel.Size = UDim2.new(0,BH_W,0,BH_H); bhPanel.Position = UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2)
    end)

    -- BotÃ£o MINIMIZAR/MAXIMIZAR
    local bhMinimized = false
    local bhMinBtn = btn(bhTopBar, "ðŸ—•", UDim2.new(0,26,0,26), UDim2.new(1,-66,0.5,-13), Color3.fromRGB(40,100,40), 12)
    bhMinBtn.ZIndex = 53
    bhMinBtn.MouseButton1Click:Connect(function()
        bhMinimized = not bhMinimized
        if bhMinimized then
            tween(bhPanel,{Size=UDim2.new(0,BH_W,0,42)},0.2)
            bhMinBtn.Text = "ðŸ—–"
        else
            tween(bhPanel,{Size=UDim2.new(0,BH_W,0,BH_H)},0.2,Enum.EasingStyle.Back,Enum.EasingDirection.Out)
            bhMinBtn.Text = "ðŸ—•"
        end
    end)

    -- BotÃ£o DIMINUIR tamanho
    local bhSizeSmall = false
    local bhSizeBtn = btn(bhTopBar, "âŠŸ", UDim2.new(0,26,0,26), UDim2.new(1,-98,0.5,-13), Color3.fromRGB(50,50,100), 12)
    bhSizeBtn.ZIndex = 53
    bhSizeBtn.MouseButton1Click:Connect(function()
        bhSizeSmall = not bhSizeSmall
        if bhSizeSmall then
            local nW,nH = 420,380
            tween(bhPanel,{Size=UDim2.new(0,nW,0,nH),Position=UDim2.new(0.5,-nW/2,0.5,-nH/2)},0.2)
            bhSizeBtn.Text = "âŠž"
        else
            tween(bhPanel,{Size=UDim2.new(0,BH_W,0,BH_H),Position=UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2)},0.2,Enum.EasingStyle.Back)
            bhSizeBtn.Text = "âŠŸ"
        end
    end)

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- MINI BOTÃƒO FLUTUANTE ARRASTAVÃ‰L (abre/fecha o painel)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhMiniBtn = Instance.new("TextButton", gui)
    bhMiniBtn.Size = UDim2.new(0,58,0,58)
    bhMiniBtn.Position = UDim2.new(1,-80,0.5,-29)
    bhMiniBtn.Text = ""; bhMiniBtn.BackgroundColor3 = Color3.fromRGB(15,50,18)
    bhMiniBtn.Active = true; bhMiniBtn.Draggable = true; bhMiniBtn.ZIndex = 60
    corner(bhMiniBtn, 29)
    local bhMiniStroke = Instance.new("UIStroke", bhMiniBtn)
    bhMiniStroke.Thickness = 2; bhMiniStroke.Color = Color3.fromRGB(50,205,50)
    -- RGB no mini botÃ£o
    task.spawn(function()
        local h2 = 100
        while bhMiniBtn and bhMiniBtn.Parent do
            h2 = (h2+1)%360; bhMiniStroke.Color = Color3.fromHSV(h2/360,1,1); task.wait(0.06)
        end
    end)
    -- Mini imagem BH dentro do botÃ£o
    local bhMiniImg = Instance.new("ImageLabel", bhMiniBtn)
    bhMiniImg.Size = UDim2.new(0,36,0,36); bhMiniImg.Position = UDim2.new(0.5,-18,0,4)
    bhMiniImg.Image = "rbxassetid://5672420098"; bhMiniImg.BackgroundTransparency = 1
    bhMiniImg.ZIndex = 61; corner(bhMiniImg, 18)
    lbl(bhMiniBtn,"BH+",UDim2.new(1,0,0,14),UDim2.new(0,0,1,-14),7,Color3.fromRGB(60,255,100)).ZIndex = 61
    -- Abrir/fechar painel
    local bhPanelOpen = false
    bhMiniBtn.MouseButton1Click:Connect(function()
        if bhPanelOpen then
            tween(bhPanel,{Size=UDim2.new(0,BH_W,0,0),Position=UDim2.new(0.5,-BH_W/2,0.5,0)},0.2)
            task.wait(0.22); bhPanel.Visible = false
            bhPanel.Size=UDim2.new(0,BH_W,0,BH_H); bhPanel.Position=UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2)
            bhPanelOpen = false
            tween(bhMiniBtn,{BackgroundColor3=Color3.fromRGB(15,50,18)},0.15)
        else
            bhPanel.Visible = true; bhPanel.Size=UDim2.new(0,0,0,0); bhPanel.Position=UDim2.new(0.5,0,0.5,0)
            tween(bhPanel,{Size=UDim2.new(0,BH_W,0,BH_H),Position=UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2)},0.3,Enum.EasingStyle.Back,Enum.EasingDirection.Out)
            bhPanelOpen = true
            tween(bhMiniBtn,{BackgroundColor3=Color3.fromRGB(20,80,25)},0.15)
        end
    end)

    -- Sidebar do painel BH
    local bhSidebar = Instance.new("Frame", bhPanel)
    bhSidebar.Size = UDim2.new(0,148,1,-44); bhSidebar.Position = UDim2.new(0,0,0,44)
    bhSidebar.BackgroundColor3 = Color3.fromRGB(16,8,28); bhSidebar.BorderSizePixel = 0; bhSidebar.ZIndex = 51
    grad(bhSidebar, Color3.fromRGB(18,9,32), Color3.fromRGB(14,6,22), 90)
    stroke(bhSidebar, Color3.fromRGB(60,20,100), 1)
    local bhSideLayout = Instance.new("UIListLayout", bhSidebar)
    bhSideLayout.Padding = UDim.new(0,3); bhSideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    local bhSidePad = Instance.new("UIPadding", bhSidebar); bhSidePad.PaddingTop = UDim.new(0,6)

    -- Content area do painel BH
    local bhContent = Instance.new("Frame", bhPanel)
    bhContent.Size = UDim2.new(1,-154,1,-50); bhContent.Position = UDim2.new(0,151,0,47)
    bhContent.BackgroundTransparency = 1; bhContent.ZIndex = 51

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- BOTÃ•ES INFERIORES (Cratera + Diminuir) - fora do painel, abaixo
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhBotBar = Instance.new("Frame", bhPanel)
    bhBotBar.Size = UDim2.new(1,0,0,36); bhBotBar.Position = UDim2.new(0,0,1,-36)
    bhBotBar.BackgroundColor3 = Color3.fromRGB(14,6,22); bhBotBar.ZIndex = 52
    corner(bhBotBar, 10); stroke(bhBotBar, Color3.fromRGB(60,20,100), 1)

    local bhCraterBtn = btn(bhBotBar, "ðŸ’¥ Cratera", UDim2.new(0.33,-3,0,28), UDim2.new(0,3,0,4), Color3.fromRGB(120,40,0), 10)
    bhCraterBtn.ZIndex = 53
    bhCraterBtn.MouseButton1Click:Connect(function()
        local char = player.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        local pos = char.HumanoidRootPart.Position
        local explosion = Instance.new("Explosion")
        explosion.Position = pos; explosion.BlastRadius = 20; explosion.BlastPressure = 1000000
        explosion.DestroyJointRadiusPercent = 0; explosion.Parent = workspace
        notify("ðŸ’¥ Cratera", "ExplosÃ£o criada!", 2)
    end)

    local bhShrinkBtn = btn(bhBotBar, "âŠŸ Diminuir", UDim2.new(0.33,-3,0,28), UDim2.new(0.33,1,0,4), Color3.fromRGB(30,30,80), 10)
    bhShrinkBtn.ZIndex = 53
    bhShrinkBtn.MouseButton1Click:Connect(function()
        bhSizeSmall = not bhSizeSmall
        if bhSizeSmall then
            local nW,nH = 420,360
            tween(bhPanel,{Size=UDim2.new(0,nW,0,nH),Position=UDim2.new(0.5,-nW/2,0.5,-nH/2)},0.2); bhSizeBtn.Text="âŠž"
        else
            tween(bhPanel,{Size=UDim2.new(0,BH_W,0,BH_H),Position=UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2)},0.2,Enum.EasingStyle.Back); bhSizeBtn.Text="âŠŸ"
        end
    end)

    local bhFecharBotBtn = btn(bhBotBar, "âœ• Fechar", UDim2.new(0.34,-3,0,28), UDim2.new(0.66,2,0,4), C.RED, 10)
    bhFecharBotBtn.ZIndex = 53
    bhFecharBotBtn.MouseButton1Click:Connect(function()
        tween(bhPanel,{Size=UDim2.new(0,BH_W,0,0),Position=UDim2.new(0.5,-BH_W/2,0.5,0)},0.2)
        task.wait(0.22); bhPanel.Visible = false
        bhPanel.Size=UDim2.new(0,BH_W,0,BH_H); bhPanel.Position=UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2); bhPanelOpen=false
    end)

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SISTEMA DE SUB-ABAS DO BH
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhTabs = {}; local bhActiveTab = nil

    local function newBhTab(name, icon)
        local tb = Instance.new("TextButton", bhSidebar)
        tb.Size = UDim2.new(0,136,0,34); tb.Text = icon.."  "..name; tb.TextSize = 10
        tb.Font = Enum.Font.GothamBold; tb.TextColor3 = Color3.fromRGB(120,60,180)
        tb.BackgroundColor3 = Color3.fromRGB(20,10,35); tb.AutoButtonColor = false
        tb.TextXAlignment = Enum.TextXAlignment.Left; tb.ZIndex = 52; corner(tb, 7)
        local pad = Instance.new("UIPadding", tb); pad.PaddingLeft = UDim.new(0,8)
        local ind = Instance.new("Frame", tb); ind.Size = UDim2.new(0,3,0.6,0); ind.Position = UDim2.new(0,0,0.2,0)
        ind.BackgroundColor3 = Color3.fromRGB(50,205,50); ind.Visible = false; ind.ZIndex = 53; corner(ind, 2)
        local page = Instance.new("ScrollingFrame", bhContent)
        page.Size = UDim2.new(1,0,1,-38); page.BackgroundTransparency = 1
        page.ScrollBarThickness = 3; page.ScrollBarImageColor3 = Color3.fromRGB(138,43,226)
        page.CanvasSize = UDim2.new(0,0,0,0); page.AutomaticCanvasSize = Enum.AutomaticSize.Y
        page.Visible = false; page.ZIndex = 51; page.BorderSizePixel = 0
        local pl = Instance.new("UIListLayout", page); pl.Padding = UDim.new(0,6); pl.SortOrder = Enum.SortOrder.LayoutOrder
        local pp = Instance.new("UIPadding", page)
        pp.PaddingTop=UDim.new(0,5); pp.PaddingLeft=UDim.new(0,5); pp.PaddingRight=UDim.new(0,5); pp.PaddingBottom=UDim.new(0,40)
        local tab = {btn=tb, page=page, ind=ind}; table.insert(bhTabs, tab)
        tb.MouseButton1Click:Connect(function()
            if bhActiveTab == tab then return end
            if bhActiveTab then
                bhActiveTab.page.Visible = false
                tween(bhActiveTab.btn,{BackgroundColor3=Color3.fromRGB(20,10,35),TextColor3=Color3.fromRGB(120,60,180)},0.15)
                bhActiveTab.ind.Visible = false
            end
            bhActiveTab = tab; page.Visible = true
            tween(tb,{BackgroundColor3=Color3.fromRGB(60,20,100),TextColor3=Color3.fromRGB(255,255,255)},0.15)
            ind.Visible = true
        end)
        return tab
    end

    local function bhSection(page, title)
        local card = Instance.new("Frame", page)
        card.Size = UDim2.new(1,0,0,0); card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = Color3.fromRGB(18,8,32); card.LayoutOrder = #page:GetChildren()
        corner(card, 9); stroke(card, Color3.fromRGB(60,20,100), 1)
        local inner = Instance.new("Frame", card)
        inner.Size = UDim2.new(1,-14,0,0); inner.Position = UDim2.new(0,7,0,0)
        inner.AutomaticSize = Enum.AutomaticSize.Y; inner.BackgroundTransparency = 1
        local il = Instance.new("UIListLayout", inner); il.Padding = UDim.new(0,5); il.SortOrder = Enum.SortOrder.LayoutOrder
        local ip = Instance.new("UIPadding", inner); ip.PaddingTop=UDim.new(0,7); ip.PaddingBottom=UDim.new(0,7)
        if title then
            local tl = lbl(inner,"â–¸ "..title,UDim2.new(1,0,0,18),nil,10,Color3.fromRGB(160,100,255),Enum.TextXAlignment.Left); tl.LayoutOrder=0
            local sep = Instance.new("Frame", inner); sep.Size=UDim2.new(1,0,0,1); sep.BackgroundColor3=Color3.fromRGB(60,20,100); sep.BorderSizePixel=0; sep.LayoutOrder=1
        end
        return inner
    end

    local function bhBtn2(parent, text, color, lo, cb)
        local b = Instance.new("TextButton", parent)
        b.Size = UDim2.new(1,0,0,30); b.BackgroundColor3 = color or Color3.fromRGB(18,55,22)
        b.Text = text; b.TextSize = 10; b.Font = Enum.Font.GothamBold
        b.TextColor3 = Color3.new(1,1,1); b.AutoButtonColor = false; b.LayoutOrder = lo or 99; b.ZIndex = 52
        corner(b, 7); stroke(b, Color3.fromRGB(50,205,50), 1)
        b.MouseEnter:Connect(function() tween(b,{BackgroundTransparency=0.2},0.1) end)
        b.MouseLeave:Connect(function() tween(b,{BackgroundTransparency=0},0.1) end)
        if cb then b.MouseButton1Click:Connect(cb) end
        return b
    end

    local function bhToggle(parent, offT, onT, lo, cb)
        local state = false
        local b = bhBtn2(parent, offT, Color3.fromRGB(30,15,55), lo)
        b.MouseButton1Click:Connect(function()
            state = not state; b.Text = state and onT or offT
            tween(b,{BackgroundColor3=state and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
            if cb then cb(state) end
        end)
        return b, function() return state end
    end

    local function bhTextbox(parent, placeholder, lo)
        local t = Instance.new("TextBox", parent)
        t.Size = UDim2.new(1,0,0,32); t.BackgroundColor3 = Color3.fromRGB(12,5,22)
        t.TextColor3 = Color3.fromRGB(230,230,255); t.TextSize = 10; t.Font = Enum.Font.Gotham
        t.PlaceholderText = placeholder or ""; t.PlaceholderColor3 = Color3.fromRGB(100,60,140)
        t.ClearTextOnFocus = false; t.LayoutOrder = lo or 99; t.TextXAlignment = Enum.TextXAlignment.Left; t.ZIndex = 53
        corner(t, 7); stroke(t, Color3.fromRGB(80,30,140), 1)
        local pad = Instance.new("UIPadding", t); pad.PaddingLeft=UDim.new(0,8); pad.PaddingRight=UDim.new(0,8)
        return t
    end

    -- VariÃ¡vel de jogador selecionado global para o painel BH
    local bhTarget = nil

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 1: PLAYERS
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhPlayersTab = newBhTab("Players", "ðŸ‘¤")
    do
        local s = bhSection(bhPlayersTab.page, "ðŸ‘¤ Selecionar Jogador")
        local pBox = bhTextbox(s, "Nome do jogador...", 2)
        local listF = Instance.new("Frame", s)
        listF.Size=UDim2.new(1,0,0,0); listF.AutomaticSize=Enum.AutomaticSize.Y
        listF.BackgroundTransparency=1; listF.LayoutOrder=3
        local listL = Instance.new("UIListLayout", listF); listL.Padding=UDim.new(0,2)

        local function refreshBhPlayers()
            for _,v in pairs(listF:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            for _,p in pairs(Players:GetPlayers()) do
                local pb = Instance.new("TextButton", listF)
                pb.Size=UDim2.new(1,0,0,26); pb.BackgroundColor3=Color3.fromRGB(20,10,35)
                pb.Text="ðŸ‘¤ "..p.Name; pb.TextSize=10; pb.Font=Enum.Font.GothamBold
                pb.TextColor3=Color3.fromRGB(180,130,255); pb.TextXAlignment=Enum.TextXAlignment.Left
                pb.AutoButtonColor=false; pb.ZIndex=53; corner(pb, 6)
                local padL=Instance.new("UIPadding",pb); padL.PaddingLeft=UDim.new(0,8)
                pb.MouseButton1Click:Connect(function()
                    pBox.Text=p.Name; bhTarget=p.Name
                    notify("Players","Selecionado: "..p.Name,1.5)
                    tween(pb,{BackgroundColor3=Color3.fromRGB(60,20,100)},0.1)
                    task.wait(0.2); tween(pb,{BackgroundColor3=Color3.fromRGB(20,10,35)},0.1)
                end)
            end
        end
        pBox.FocusLost:Connect(function() bhTarget = pBox.Text end)
        bhBtn2(s,"ðŸ”„ Atualizar Lista",Color3.fromRGB(30,15,55),4,refreshBhPlayers)
        refreshBhPlayers()

        local s2 = bhSection(bhPlayersTab.page, "ðŸŽ¯ AÃ§Ãµes do Jogador")
        bhBtn2(s2,"ðŸš€ Teleport â†’ Jogador",Color3.fromRGB(18,55,80),2,function()
            local name = pBox.Text ~= "" and pBox.Text or bhTarget; if not name then return end
            local t=Players:FindFirstChild(name)
            if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") then
                local myChar=player.Character
                if myChar and myChar:FindFirstChild("HumanoidRootPart") then
                    myChar.HumanoidRootPart.CFrame = t.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,-3)
                    notify("TP","Teleportado para "..name,2)
                end
            end
        end)
        bhBtn2(s2,"ðŸ“· Visualizar Jogador",Color3.fromRGB(18,55,80),3,function()
            local name = pBox.Text ~= "" and pBox.Text or bhTarget; if not name then return end
            local t=Players:FindFirstChild(name)
            if t and t.Character and t.Character:FindFirstChild("Humanoid") then
                workspace.CurrentCamera.CameraSubject=t.Character.Humanoid; notify("CÃ¢mera","Visualizando "..name,2)
            end
        end)
        bhBtn2(s2,"ðŸ”™ Voltar CÃ¢mera",Color3.fromRGB(30,15,55),4,function()
            if player.Character and player.Character:FindFirstChild("Humanoid") then
                workspace.CurrentCamera.CameraSubject=player.Character.Humanoid; notify("CÃ¢mera","Voltou!",1.5)
            end
        end)
        bhBtn2(s2,"ðŸ’€ Matar Jogador",Color3.fromRGB(100,15,15),5,function()
            local name = pBox.Text ~= "" and pBox.Text or bhTarget; if not name then return end
            local t=Players:FindFirstChild(name)
            if t and t.Character then
                local hum=t.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum.Health=0; notify("Kill","â˜ ï¸ "..name.." morreu!",2) end
            end
        end)
        bhBtn2(s2,"â¬†ï¸ Jogar Jogador p/ Cima",Color3.fromRGB(60,30,10),6,function()
            local name = pBox.Text ~= "" and pBox.Text or bhTarget; if not name then return end
            local t=Players:FindFirstChild(name)
            if t and t.Character then
                local hrp=t.Character:FindFirstChild("HumanoidRootPart")
                if hrp then hrp.AssemblyLinearVelocity=Vector3.new(0,300,0); notify("Fling","â†‘ "..name.." voou!",2) end
            end
        end)
        bhBtn2(s2,"ðŸŒ€ Fling (Girar)",Color3.fromRGB(80,10,80),7,function()
            local name = pBox.Text ~= "" and pBox.Text or bhTarget; if not name then return end
            local t=Players:FindFirstChild(name)
            if t and t.Character then
                local hrp=t.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    hrp.AssemblyLinearVelocity=Vector3.new(math.random(-200,200),200,math.random(-200,200))
                    notify("Fling","ðŸŒ€ "..name.." girou!",2)
                end
            end
        end)
        bhBtn2(s2,"ðŸª‘ ForÃ§ar Sentar",Color3.fromRGB(30,55,18),8,function()
            local name = pBox.Text ~= "" and pBox.Text or bhTarget; if not name then return end
            local t=Players:FindFirstChild(name)
            if t and t.Character then
                local hum=t.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum.Sit=true; notify("Sentar","ðŸª‘ "..name.." sentou!",2) end
            end
        end)

        local s3 = bhSection(bhPlayersTab.page, "ðŸ‘¥ AÃ§Ãµes em TODOS")
        bhBtn2(s3,"ðŸ’€ Matar TODOS",Color3.fromRGB(120,0,0),2,function()
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hum=p.Character:FindFirstChildOfClass("Humanoid")
                    if hum then hum.Health=0 end
                end
            end; notify("Kill All","â˜ ï¸ Todos morreram!",3)
        end)
        bhBtn2(s3,"â¬†ï¸ Jogar TODOS p/ Cima",Color3.fromRGB(60,30,10),3,function()
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hrp=p.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then hrp.AssemblyLinearVelocity=Vector3.new(math.random(-50,50),300,math.random(-50,50)) end
                end
            end; notify("Fling All","â†‘ Todos voaram!",3)
        end)
        bhBtn2(s3,"ðŸŒ€ Fling TODOS",Color3.fromRGB(80,10,80),4,function()
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hrp=p.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then hrp.AssemblyLinearVelocity=Vector3.new(math.random(-300,300),200,math.random(-300,300)) end
                end
            end; notify("Fling All","ðŸŒ€ Todos giraram!",3)
        end)
        bhBtn2(s3,"ðŸ”” Puxar TODOS para Mim",Color3.fromRGB(18,55,80),5,function()
            local myHRP=player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if not myHRP then return end
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hrp=p.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then hrp.CFrame=myHRP.CFrame*CFrame.new(math.random(-5,5),0,math.random(-5,5)) end
                end
            end; notify("Bring All","âœ… Todos chegaram!",2)
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 2: AVATAR / PLAYER
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhAvatarTab = newBhTab("Avatar", "ðŸ§")
    do
        local s = bhSection(bhAvatarTab.page, "âš¡ Velocidade")
        local spd = 16
        local spdLbl = lbl(s,"âš¡ Speed: "..spd,UDim2.new(1,0,0,22),nil,11,Color3.fromRGB(200,150,255)); spdLbl.LayoutOrder=2
        local function applySpd()
            local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.WalkSpeed=spd end; spdLbl.Text="âš¡ Speed: "..spd
        end
        local row=Instance.new("Frame",s); row.Size=UDim2.new(1,0,0,30); row.BackgroundTransparency=1; row.LayoutOrder=3
        local bm=Instance.new("TextButton",row); bm.Size=UDim2.new(0.5,-2,1,0); bm.BackgroundColor3=C.RED; bm.Text="-10"; bm.TextSize=11; bm.Font=Enum.Font.GothamBold; bm.TextColor3=C.WHITE; bm.AutoButtonColor=false; corner(bm,7)
        bm.MouseButton1Click:Connect(function() spd=math.max(5,spd-10); applySpd() end)
        local bp=Instance.new("TextButton",row); bp.Size=UDim2.new(0.5,-2,1,0); bp.Position=UDim2.new(0.5,2,0,0); bp.BackgroundColor3=C.GREEN; bp.Text="+10"; bp.TextSize=11; bp.Font=Enum.Font.GothamBold; bp.TextColor3=C.WHITE; bp.AutoButtonColor=false; corner(bp,7)
        bp.MouseButton1Click:Connect(function() spd=math.min(1000,spd+10); applySpd() end)
        -- Preset speeds
        local rowP=Instance.new("Frame",s); rowP.Size=UDim2.new(1,0,0,26); rowP.BackgroundTransparency=1; rowP.LayoutOrder=4
        local rL=Instance.new("UIListLayout",rowP); rL.FillDirection=Enum.FillDirection.Horizontal; rL.Padding=UDim.new(0,3)
        for _,v in ipairs({{t="ðŸ¢16",v=16},{t="ðŸš¶50",v=50},{t="ðŸƒ100",v=100},{t="âš¡200",v=200}}) do
            local pb=Instance.new("TextButton",rowP); pb.Size=UDim2.new(0.25,-3,1,0); pb.BackgroundColor3=Color3.fromRGB(30,15,55); pb.Text=v.t; pb.TextSize=9; pb.Font=Enum.Font.GothamBold; pb.TextColor3=C.WHITE; pb.AutoButtonColor=false; corner(pb,6)
            pb.MouseButton1Click:Connect(function() spd=v.v; applySpd() end)
        end

        local sJ = bhSection(bhAvatarTab.page, "â†• Pulo")
        local jp = 50
        local jpLbl=lbl(sJ,"â†• Jump: "..jp,UDim2.new(1,0,0,22),nil,11,Color3.fromRGB(200,150,255)); jpLbl.LayoutOrder=2
        local function applyJP()
            local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.JumpPower=jp end; jpLbl.Text="â†• Jump: "..jp
        end
        local rowJ=Instance.new("Frame",sJ); rowJ.Size=UDim2.new(1,0,0,30); rowJ.BackgroundTransparency=1; rowJ.LayoutOrder=3
        local jm=Instance.new("TextButton",rowJ); jm.Size=UDim2.new(0.5,-2,1,0); jm.BackgroundColor3=C.RED; jm.Text="-25"; jm.TextSize=11; jm.Font=Enum.Font.GothamBold; jm.TextColor3=C.WHITE; jm.AutoButtonColor=false; corner(jm,7)
        jm.MouseButton1Click:Connect(function() jp=math.max(0,jp-25); applyJP() end)
        local jpp=Instance.new("TextButton",rowJ); jpp.Size=UDim2.new(0.5,-2,1,0); jpp.Position=UDim2.new(0.5,2,0,0); jpp.BackgroundColor3=C.GREEN; jpp.Text="+25"; jpp.TextSize=11; jpp.Font=Enum.Font.GothamBold; jpp.TextColor3=C.WHITE; jpp.AutoButtonColor=false; corner(jpp,7)
        jpp.MouseButton1Click:Connect(function() jp=math.min(500,jp+25); applyJP() end)

        local s2 = bhSection(bhAvatarTab.page, "ðŸŽ® MovimentaÃ§Ã£o Extra")
        local noclipBh=false
        local ncBtn=bhBtn2(s2,"ðŸš« Noclip: OFF",Color3.fromRGB(30,15,55),2)
        ncBtn.MouseButton1Click:Connect(function()
            noclipBh=not noclipBh; ncBtn.Text=noclipBh and "âœ… Noclip: ON" or "ðŸš« Noclip: OFF"
            tween(ncBtn,{BackgroundColor3=noclipBh and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
        end)
        RunService.Stepped:Connect(function()
            if noclipBh and player.Character then
                for _,v in pairs(player.Character:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide=false end end
            end
        end)

        local infJump=false
        local ijBtn=bhBtn2(s2,"â™¾ Inf Jump: OFF",Color3.fromRGB(30,15,55),3)
        ijBtn.MouseButton1Click:Connect(function()
            infJump=not infJump; ijBtn.Text=infJump and "â™¾ Inf Jump: ON" or "â™¾ Inf Jump: OFF"
            tween(ijBtn,{BackgroundColor3=infJump and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
        end)
        local uis=game:GetService("UserInputService")
        uis.JumpRequest:Connect(function()
            if infJump then
                local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
            end
        end)

        -- Fly
        local flying=false; local flyBV=nil; local flySpeed=50
        local flyBtn=bhBtn2(s2,"ðŸ›¸ Fly: OFF",Color3.fromRGB(30,15,55),4)
        flyBtn.MouseButton1Click:Connect(function()
            flying=not flying; flyBtn.Text=flying and "ðŸ›¸ Fly: ON" or "ðŸ›¸ Fly: OFF"
            tween(flyBtn,{BackgroundColor3=flying and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
            if flying then
                local char=player.Character; if not char then return end
                local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
                local hum=char:FindFirstChildOfClass("Humanoid"); if hum then hum.PlatformStand=true end
                flyBV=Instance.new("BodyVelocity",hrp); flyBV.MaxForce=Vector3.new(1e9,1e9,1e9); flyBV.Velocity=Vector3.new(0,0,0)
                local flyBG=Instance.new("BodyGyro",hrp); flyBG.MaxTorque=Vector3.new(1e9,1e9,1e9); flyBG.D=500
                task.spawn(function()
                    while flying and flyBV and flyBV.Parent do
                        local cam=workspace.CurrentCamera; local cf=cam.CFrame
                        local mov=Vector3.new(0,0,0)
                        if uis:IsKeyDown(Enum.KeyCode.W) then mov=mov+cf.LookVector*flySpeed end
                        if uis:IsKeyDown(Enum.KeyCode.S) then mov=mov-cf.LookVector*flySpeed end
                        if uis:IsKeyDown(Enum.KeyCode.A) then mov=mov-cf.RightVector*flySpeed end
                        if uis:IsKeyDown(Enum.KeyCode.D) then mov=mov+cf.RightVector*flySpeed end
                        if uis:IsKeyDown(Enum.KeyCode.Space) then mov=mov+Vector3.new(0,flySpeed,0) end
                        if uis:IsKeyDown(Enum.KeyCode.LeftShift) then mov=mov-Vector3.new(0,flySpeed,0) end
                        flyBV.Velocity=mov; flyBG.CFrame=cam.CFrame
                        task.wait()
                    end
                end)
            else
                if flyBV and flyBV.Parent then flyBV.Parent:FindFirstChildOfClass("BodyGyro"):Destroy(); flyBV:Destroy() end; flyBV=nil
                local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid"); if hum then hum.PlatformStand=false end
            end
        end)

        local s3=bhSection(bhAvatarTab.page,"ðŸŒˆ AparÃªncia")
        bhBtn2(s3,"ðŸ‘» InvisÃ­vel: Toggle",Color3.fromRGB(30,30,60),2,function()
            local char=player.Character; if not char then return end
            for _,v in pairs(char:GetDescendants()) do
                if v:IsA("BasePart") and v.Name ~= "HumanoidRootPart" then
                    v.Transparency = v.Transparency >= 0.9 and 0 or 1
                end
                if v:IsA("Decal") then v.Transparency = v.Transparency >= 0.9 and 0 or 1 end
            end
            notify("Avatar","ðŸ‘» Invisibilidade alternada!",2)
        end)
        bhBtn2(s3,"ðŸ”„ Reset Avatar",Color3.fromRGB(80,20,20),3,function()
            local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.Health=0; notify("Avatar","ðŸ”„ Resetando...",2) end
        end)
        bhBtn2(s3,"ðŸ“ Gigante",Color3.fromRGB(50,25,0),4,function()
            local char=player.Character; if not char then return end
            local hum=char:FindFirstChildOfClass("Humanoid"); if hum then hum.BodyDepthScale.Value=3; hum.BodyHeightScale.Value=3; hum.BodyWidthScale.Value=3; hum.HeadScale.Value=3; notify("Avatar","ðŸ“ Gigante!",2) end
        end)
        bhBtn2(s3,"ðŸ£ MinÃºsculo",Color3.fromRGB(25,50,0),5,function()
            local char=player.Character; if not char then return end
            local hum=char:FindFirstChildOfClass("Humanoid"); if hum then hum.BodyDepthScale.Value=0.1; hum.BodyHeightScale.Value=0.1; hum.BodyWidthScale.Value=0.1; hum.HeadScale.Value=0.1; notify("Avatar","ðŸ£ MinÃºsculo!",2) end
        end)
        bhBtn2(s3,"â†©ï¸ Tamanho Normal",Color3.fromRGB(18,55,22),6,function()
            local char=player.Character; if not char then return end
            local hum=char:FindFirstChildOfClass("Humanoid"); if hum then hum.BodyDepthScale.Value=1; hum.BodyHeightScale.Value=1; hum.BodyWidthScale.Value=1; hum.HeadScale.Value=1; notify("Avatar","âœ… Normal!",2) end
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 3: HOUSE (Casa/Build)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhHouseTab = newBhTab("House", "ðŸ ")
    do
        local RE = ReplicatedStorage:FindFirstChild("RE")
        local lastObj = nil

        local s = bhSection(bhHouseTab.page, "ðŸ  Teleportes de Casa")
        local houseDestinations = {
            {name="Casa 1", pos=Vector3.new(-50,5,-30)},
            {name="Casa 2", pos=Vector3.new(-25,5,-30)},
            {name="Casa 3", pos=Vector3.new(0,5,-30)},
            {name="Supermercado", pos=Vector3.new(-80,5,-70)},
            {name="Parque", pos=Vector3.new(80,5,70)},
            {name="Centro", pos=Vector3.new(0,5,0)},
            {name="Escola", pos=Vector3.new(50,5,-50)},
            {name="Hospital", pos=Vector3.new(-60,5,60)},
            {name="Aeroporto", pos=Vector3.new(120,5,30)},
            {name="Delegacia", pos=Vector3.new(-30,5,80)},
            {name="Ilha", pos=Vector3.new(200,5,200)},
            {name="AgÃªncia", pos=Vector3.new(-100,5,-100)},
            {name="Passagem Secreta", pos=Vector3.new(71,-84,67)},
        }
        for _,dest in ipairs(houseDestinations) do
            bhBtn2(s,"ðŸ“ "..dest.name,Color3.fromRGB(18,55,80),#s:GetChildren()+1,function()
                local char=player.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    char.HumanoidRootPart.CFrame=CFrame.new(dest.pos)
                    notify("TP ðŸ ",dest.name.."!",2)
                end
            end)
        end

        local s2 = bhSection(bhHouseTab.page, "ðŸ—ï¸ Build")
        bhBtn2(s2,"ðŸ§± Criar Parede",Color3.fromRGB(18,55,22),2,function()
            local char=player.Character; if not char or not char:FindFirstChild("HumanoidRootPart") then return end
            local w=Instance.new("Part"); w.Size=Vector3.new(14,10,1)
            w.Position=char.HumanoidRootPart.Position+char.HumanoidRootPart.CFrame.LookVector*8
            w.Anchored=true; w.Material=Enum.Material.SmoothPlastic
            w.Color=Color3.fromRGB(math.random(50,255),math.random(50,255),math.random(50,255))
            w.Parent=workspace; lastObj=w; notify("Build","ðŸ§± Parede criada!",2)
        end)
        bhBtn2(s2,"ðŸŸ« Criar Tapete",Color3.fromRGB(18,55,22),3,function()
            local char=player.Character; if not char or not char:FindFirstChild("HumanoidRootPart") then return end
            local c=Instance.new("Part"); c.Size=Vector3.new(20,0.5,20)
            c.Position=char.HumanoidRootPart.Position+Vector3.new(0,-3,0)
            c.Anchored=true; c.Material=Enum.Material.Fabric
            c.Color=Color3.fromRGB(math.random(50,255),math.random(50,255),math.random(50,255))
            c.Parent=workspace; lastObj=c; notify("Build","ðŸŸ« Tapete criado!",2)
        end)
        bhBtn2(s2,"ðŸ”† Plataforma Brilhante",Color3.fromRGB(50,25,0),4,function()
            local char=player.Character; if not char or not char:FindFirstChild("HumanoidRootPart") then return end
            local p=Instance.new("Part"); p.Size=Vector3.new(10,0.5,10)
            p.Position=char.HumanoidRootPart.Position+Vector3.new(0,-2,0)
            p.Anchored=true; p.Material=Enum.Material.Neon
            p.Color=Color3.fromRGB(math.random(50,255),math.random(50,255),math.random(50,255))
            p.Parent=workspace; lastObj=p; notify("Build","ðŸ”† Plataforma neon!",2)
        end)
        bhBtn2(s2,"ðŸ’¥ Criar Cratera",Color3.fromRGB(80,30,0),5,function()
            local char=player.Character; if not char or not char:FindFirstChild("HumanoidRootPart") then return end
            local pos=char.HumanoidRootPart.Position
            local exp=Instance.new("Explosion"); exp.Position=pos; exp.BlastRadius=25
            exp.BlastPressure=1500000; exp.DestroyJointRadiusPercent=0; exp.Parent=workspace
            notify("Build","ðŸ’¥ Cratera criada!",2)
        end)
        bhBtn2(s2,"ðŸŽ¨ Cor AleatÃ³ria",Color3.fromRGB(80,20,120),6,function()
            if lastObj and lastObj.Parent then
                lastObj.Color=Color3.fromRGB(math.random(50,255),math.random(50,255),math.random(50,255))
                notify("Build","ðŸŽ¨ Cor alterada!",1)
            end
        end)
        bhBtn2(s2,"ðŸ—‘ Deletar Ãšltimo",C.RED,7,function()
            if lastObj and lastObj.Parent then lastObj:Destroy(); lastObj=nil; notify("Build","ðŸ—‘ Deletado!",2) end
        end)
        bhBtn2(s2,"ðŸ—‘ Deletar Tudo Meu",Color3.fromRGB(100,20,20),8,function()
            local deleted=0
            for _,v in pairs(workspace:GetChildren()) do
                if (v.Name=="AvenWall" or v.Name=="AvenCarpet") and v:IsA("BasePart") then v:Destroy(); deleted=deleted+1 end
            end
            notify("Build","ðŸ—‘ "..deleted.." objetos deletados!",3)
        end)

        local s3 = bhSection(bhHouseTab.page, "ðŸ”Š Som na Casa (RemoteEvent)")
        local musicBox = bhTextbox(s3,"ID de mÃºsica para tocar na casa...",2)
        bhBtn2(s3,"â–¶ï¸ Tocar MÃºsica na Casa",Color3.fromRGB(18,55,22),3,function()
            if musicBox.Text=="" then return end
            pcall(function()
                local remote=ReplicatedStorage:FindFirstChild("RemoteEvents") and ReplicatedStorage.RemoteEvents:FindFirstChild("PlayersHouse")
                if remote then remote:FireServer("sound",musicBox.Text); notify("MÃºsica","â–¶ï¸ Tocando!",2)
                else notify("Erro","Remote nÃ£o encontrado!",2) end
            end)
        end)
        bhBtn2(s3,"â¹ Parar MÃºsica",Color3.fromRGB(80,20,20),4,function()
            pcall(function()
                local remote=ReplicatedStorage:FindFirstChild("RemoteEvents") and ReplicatedStorage.RemoteEvents:FindFirstChild("PlayersHouse")
                if remote then remote:FireServer("sound","0"); notify("MÃºsica","â¹ Parado!",2) end
            end)
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 4: CAR (Carro)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhCarTab = newBhTab("Car", "ðŸš—")
    do
        local s = bhSection(bhCarTab.page, "ðŸŽ¯ Alvo")
        local carBox = bhTextbox(s,"Nome do jogador alvo...",2)
        local listFC=Instance.new("Frame",s); listFC.Size=UDim2.new(1,0,0,0); listFC.AutomaticSize=Enum.AutomaticSize.Y; listFC.BackgroundTransparency=1; listFC.LayoutOrder=3
        local listLC=Instance.new("UIListLayout",listFC); listLC.Padding=UDim.new(0,2)
        local function refreshCarList()
            for _,v in pairs(listFC:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            for _,p in pairs(Players:GetPlayers()) do
                local pb=Instance.new("TextButton",listFC); pb.Size=UDim2.new(1,0,0,24); pb.BackgroundColor3=Color3.fromRGB(20,10,35)
                pb.Text="ðŸŽ¯ "..p.Name; pb.TextSize=9; pb.Font=Enum.Font.GothamBold; pb.TextColor3=Color3.fromRGB(180,130,255)
                pb.TextXAlignment=Enum.TextXAlignment.Left; pb.AutoButtonColor=false; pb.ZIndex=53; corner(pb,5)
                local padL=Instance.new("UIPadding",pb); padL.PaddingLeft=UDim.new(0,8)
                pb.MouseButton1Click:Connect(function() carBox.Text=p.Name; bhTarget=p.Name; notify("Car","Alvo: "..p.Name,1.5) end)
            end
        end
        bhBtn2(s,"ðŸ”„ Atualizar",Color3.fromRGB(30,15,55),4,refreshCarList)
        refreshCarList()

        local function doCarAction(targetName, actionType)
            notify("Car","Iniciando "..actionType.."...",2)
            task.spawn(function()
                pcall(function()
                    local Vehicles=Workspace:FindFirstChild("Vehicles"); if not Vehicles then notify("Erro","Sem Vehicles!",2); return end
                    local myChar=player.Character; if not myChar then return end
                    local HRP=myChar:FindFirstChild("HumanoidRootPart"); local Hum=myChar:FindFirstChildOfClass("Humanoid")
                    if not HRP or not Hum then return end
                    local OldPos=HRP.CFrame
                    local PCar=Vehicles:FindFirstChild(player.Name.."Car")
                    if not PCar then
                        HRP.CFrame=CFrame.new(1118.81,75.998,-1138.61); task.wait(0.5)
                        local remote=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1Ca1r")
                        if remote then remote:FireServer("PickingCar","SchoolBus"); task.wait(0.5) end
                        PCar=Vehicles:FindFirstChild(player.Name.."Car")
                    end
                    if not PCar then notify("Erro","Ã”nibus nÃ£o encontrado!",2); return end
                    local Seat=PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
                    if Seat and not Hum.Sit then
                        local att=0
                        repeat task.wait(); HRP.CFrame=Seat.CFrame*CFrame.new(0,math.random(-1,1),0); att=att+1 until Hum.Sit or att>30
                    end
                    task.wait(0.3)
                    local goTogether=(targetName==player.Name or targetName==player.DisplayName)
                    if goTogether then
                        if actionType=="Bring" then
                            if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(HRP.Position+Vector3.new(0,10,0))) end
                        elseif actionType=="Passagem" then
                            if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(Vector3.new(71,-84,67))) end
                            task.wait(1); notify("Kill Bus","ðŸ—ï¸ Passagem Secreta!",3)
                        end
                    else
                        local TargetPlayer=Players:FindFirstChild(targetName); if not TargetPlayer then notify("Erro","Jogador nÃ£o encontrado!",2); Hum.Sit=false; HRP.CFrame=OldPos; return end
                        local TargetC=TargetPlayer.Character; local TargetH=TargetC and TargetC:FindFirstChildOfClass("Humanoid"); local TargetRP=TargetC and TargetC:FindFirstChild("HumanoidRootPart")
                        if not TargetH or not TargetRP then notify("Erro","Jogador sem char!",2); Hum.Sit=false; HRP.CFrame=OldPos; return end
                        local attempts=0; local maxA=120
                        while not TargetH.Sit and attempts<maxA and TargetH.Health>0 do
                            attempts=attempts+1; task.wait()
                            if not PCar or not PCar.PrimaryPart then break end
                            local rX,rY,rZ=math.random(-360,360),math.random(-360,360),math.random(-360,360)
                            local function cap(off,ang) pcall(function() PCar:SetPrimaryPartCFrame(CFrame.new(TargetRP.Position)*off*ang) end) end
                            cap(CFrame.new(0,1,0)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                            cap(CFrame.new(0,-2,4)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                        end
                        if TargetH.Sit then
                            task.wait(0.3)
                            if actionType=="Bring" then
                                notify("Car Bring","âœ… "..targetName.." capturado!",3)
                            elseif actionType=="Kill" then
                                if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(0,-470,0)) end
                                task.wait(0.2); Hum.Sit=false; task.wait(0.1); HRP.CFrame=OldPos
                                notify("Car Kill","â˜ ï¸ "..targetName.." morreu!",3)
                            elseif actionType=="Passagem" then
                                if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(Vector3.new(71,-84,67))) end
                                task.wait(1)
                                if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(0,-470,0)) end
                                task.wait(0.2); Hum.Sit=false; task.wait(0.1); HRP.CFrame=OldPos
                                notify("Kill Bus","ðŸ—ï¸ "..targetName.." na Passagem!",3)
                            end
                        else notify("Car","âŒ NÃ£o conseguiu capturar!",2); Hum.Sit=false; task.wait(0.1); HRP.CFrame=OldPos end
                    end
                end)
            end)
        end

        local s2 = bhSection(bhCarTab.page, "ðŸšŒ AÃ§Ãµes com Ã”nibus")
        bhBtn2(s2,"ðŸš— Car Bring (Pegar)",Color3.fromRGB(18,55,22),2,function()
            local n=carBox.Text ~= "" and carBox.Text or bhTarget; if not n then notify("Erro","Selecione alvo!",2); return end
            doCarAction(n,"Bring")
        end)
        bhBtn2(s2,"ðŸ’€ Car Kill (Matar)",Color3.fromRGB(100,15,15),3,function()
            local n=carBox.Text ~= "" and carBox.Text or bhTarget; if not n then notify("Erro","Selecione alvo!",2); return end
            doCarAction(n,"Kill")
        end)
        bhBtn2(s2,"ðŸ—ï¸ Kill Bus 02 (Passagem Secreta)",Color3.fromRGB(80,0,120),4,function()
            local n=carBox.Text ~= "" and carBox.Text or bhTarget; if not n then notify("Erro","Selecione alvo!",2); return end
            doCarAction(n,"Passagem")
        end)

        local s3=bhSection(bhCarTab.page,"ðŸ’€ Kill Bus 03 - CÃ³digo")
        local kb3Box=bhTextbox(s3,"Nome do jogador...",2)
        local kb3Code=bhTextbox(s3,"CÃ³digo KB03|X|Y|Z",3)
        bhBtn2(s3,"ðŸ“ Gerar CÃ³digo (Pos Atual)",C.ORANGE,4,function()
            local char=player.Character; if char and char:FindFirstChild("HumanoidRootPart") then
                local pos=char.HumanoidRootPart.Position
                local code=string.format("KB03|%.2f|%.2f|%.2f",pos.X,pos.Y,pos.Z)
                kb3Code.Text=code; setclipboard(code); notify("CÃ³digo","ðŸ“‹ Copiado!",2)
            end
        end)
        -- CÃ³digos rÃ¡pidos
        local quickKB = {{"Passagem","KB03|71.00|-84.00|67.00"},{"Casa 1","KB03|-50.00|5.00|-30.00"},{"Escola","KB03|50.00|5.00|-50.00"},{"Hospital","KB03|-60.00|5.00|60.00"}}
        for i,q in ipairs(quickKB) do
            local qb=bhBtn2(s3,"ðŸ“‹ "..q[1],Color3.fromRGB(30,15,55),i+4,function()
                kb3Code.Text=q[2]; setclipboard(q[2]); notify("CÃ³digo",q[1].." copiado!",2)
            end)
        end
        bhBtn2(s3,"ðŸ’€ KILL BUS 03 - Executar",Color3.fromRGB(150,0,120),9,function()
            local targetName=kb3Box.Text; local code=kb3Code.Text
            if targetName=="" then notify("Erro","Digite o jogador!",2); return end
            if code=="" then notify("Erro","Cole o cÃ³digo!",2); return end
            local parts=string.split(code,"|")
            if #parts~=4 or parts[1]~="KB03" then notify("Erro","CÃ³digo invÃ¡lido!",2); return end
            local destX,destY,destZ=tonumber(parts[2]),tonumber(parts[3]),tonumber(parts[4])
            if not destX then notify("Erro","Coordenadas invÃ¡lidas!",2); return end
            notify("Kill Bus 03","ðŸšŒ Iniciando sequÃªncia...",3)
            task.spawn(function()
                pcall(function()
                    local Character=player.Character; if not Character then return end
                    local Humanoid=Character:FindFirstChildOfClass("Humanoid"); local RootPart=Character:FindFirstChild("HumanoidRootPart"); local Vehicles=Workspace:FindFirstChild("Vehicles")
                    if not Humanoid or not RootPart or not Vehicles then return end
                    local OldPos=RootPart.CFrame
                    RootPart.CFrame=CFrame.new(500,150,500); task.wait(0.5)
                    local PCar=Vehicles:FindFirstChild(player.Name.."Car")
                    if not PCar then
                        RootPart.CFrame=CFrame.new(1118.81,75.998,-1138.61); task.wait(0.5)
                        local remote=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1Ca1r")
                        if remote then remote:FireServer("PickingCar","SchoolBus"); task.wait(0.6) end
                        PCar=Vehicles:FindFirstChild(player.Name.."Car")
                    end
                    if not PCar then notify("Erro","Sem Ã´nibus!",2); RootPart.CFrame=OldPos; return end
                    local Seat=PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
                    if Seat then
                        local sa=0
                        repeat task.wait(); RootPart.CFrame=Seat.CFrame*CFrame.new(0,math.random(-1,1),0); sa=sa+1 until Humanoid.Sit or not Seat.Parent or sa>30
                    end
                    task.wait(0.3)
                    local TargetPlayer=Players:FindFirstChild(targetName); if not TargetPlayer then notify("Erro","NÃ£o encontrado!",2); Humanoid.Sit=false; RootPart.CFrame=OldPos; return end
                    local TargetC=TargetPlayer.Character; local TargetH=TargetC and TargetC:FindFirstChildOfClass("Humanoid"); local TargetRP=TargetC and TargetC:FindFirstChild("HumanoidRootPart")
                    if not TargetH or not TargetRP then notify("Erro","Sem personagem!",2); Humanoid.Sit=false; RootPart.CFrame=OldPos; return end
                    local attempts=0
                    while not TargetH.Sit and attempts<100 and TargetH.Health>0 do
                        attempts=attempts+1; task.wait()
                        if not PCar or not PCar.PrimaryPart then break end
                        local rX,rY,rZ=math.random(-360,360),math.random(-360,360),math.random(-360,360)
                        local function capF(off,ang) pcall(function() PCar:SetPrimaryPartCFrame(CFrame.new(TargetRP.Position)*off*ang) end) end
                        capF(CFrame.new(0,1,0)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                        capF(CFrame.new(0,-2,4)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                    end
                    if TargetH.Sit then
                        task.wait(0.3); notify("Kill Bus 03","âœ… Capturado! Levando...",2)
                        local destPos=Vector3.new(destX,destY,destZ)
                        if PCar and PCar.PrimaryPart then
                            local mid=Vector3.new((RootPart.Position.X+destX)/2,math.max(RootPart.Position.Y,destY)+50,(RootPart.Position.Z+destZ)/2)
                            PCar:SetPrimaryPartCFrame(CFrame.new(mid)); task.wait(0.3); PCar:SetPrimaryPartCFrame(CFrame.new(destPos))
                        end
                        task.wait(1)
                        if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(0,-470,0)) end
                        task.wait(0.3); Humanoid.Sit=false; task.wait(0.2); RootPart.CFrame=OldPos
                        notify("Kill Bus 03","ðŸŽ¯ "..targetName.." chegou!",3)
                    else
                        notify("Kill Bus 03","âŒ Falhou!",2); Humanoid.Sit=false; task.wait(0.2); RootPart.CFrame=OldPos
                    end
                end)
            end)
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 5: RGB (AnimaÃ§Ãµes Visuais)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhRgbTab = newBhTab("RGB", "ðŸŒˆ")
    do
        local s = bhSection(bhRgbTab.page, "ðŸŒˆ Efeitos Visuais")
        local rainbowName = false
        local rainbowActive = false
        bhToggle(s,"ðŸŒˆ Rainbow Nome: OFF","ðŸŒˆ Rainbow Nome: ON",2,function(state)
            rainbowName=state
            task.spawn(function()
                local h3=0
                while rainbowName do
                    h3=(h3+2)%360
                    local col=Color3.fromHSV(h3/360,1,1)
                    pcall(function()
                        local remote=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1RPNam1eTex1t")
                        if remote then
                            local r,g,b=math.floor(col.R*255),math.floor(col.G*255),math.floor(col.B*255)
                            remote:FireServer("RolePlayBio","<font color='rgb("..r..","..g..","..b..")'>"..player.Name.."</font>")
                        end
                    end)
                    task.wait(0.15)
                end
            end)
        end)

        local rainbowBio=false
        bhToggle(s,"ðŸŒˆ Rainbow Bio: OFF","ðŸŒˆ Rainbow Bio: ON",3,function(state)
            rainbowBio=state
            task.spawn(function()
                local h4=180
                while rainbowBio do
                    h4=(h4+3)%360
                    local col=Color3.fromHSV(h4/360,1,1)
                    local r,g,b=math.floor(col.R*255),math.floor(col.G*255),math.floor(col.B*255)
                    pcall(function()
                        local remote=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1RPNam1eTex1t")
                        if remote then remote:FireServer("RolePlayBio","<font color='rgb("..r..","..g..","..b..")'>AvenHub V20 ðŸ”¥</font>") end
                    end)
                    task.wait(0.2)
                end
            end)
        end)

        local s2=bhSection(bhRgbTab.page,"âœï¸ Nome & Bio Custom")
        local nameBox=bhTextbox(s2,"Novo nome...",2)
        local bioBox=bhTextbox(s2,"Nova bio...",3)
        bhBtn2(s2,"âœ… Aplicar Nome/Bio",Color3.fromRGB(18,55,22),4,function()
            pcall(function()
                local remote=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1RPNam1eTex1t")
                if remote then
                    if nameBox.Text~="" then remote:FireServer("RolePlayName",nameBox.Text) end
                    if bioBox.Text~="" then remote:FireServer("RolePlayBio",bioBox.Text) end
                    notify("Nome/Bio","âœ… Aplicado!",2)
                end
            end)
        end)
        bhBtn2(s2,"ðŸ”„ Resetar Nome/Bio",Color3.fromRGB(80,20,20),5,function()
            pcall(function()
                local remote=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1RPNam1eTex1t")
                if remote then remote:FireServer("RolePlayName",player.Name); remote:FireServer("RolePlayBio","") end
                notify("Nome/Bio","ðŸ”„ Resetado!",2)
            end)
        end)

        local s3=bhSection(bhRgbTab.page,"ðŸŽ­ Tags / Empregos")
        local jobIds = {
            {name="ðŸŸ¡ Admin Logo",id="1151106808"}, {name="ðŸ˜ˆ Hacker Face",id="3284478282"},
            {name="ðŸ‘® Policial",id="3570718609"}, {name="ðŸš’ Bombeiro",id="3570719252"},
            {name="ðŸ©º MÃ©dico",id="3570720036"}, {name="ðŸŽ“ Professor",id="3570719567"},
        }
        for i,job in ipairs(jobIds) do
            bhBtn2(s3,job.name,Color3.fromRGB(30,15,55),i+1,function()
                pcall(function()
                    local remote=ReplicatedStorage:FindFirstChild("RemoteEvents") and ReplicatedStorage.RemoteEvents:FindFirstChild("J1obs11335")
                    if remote then remote:FireServer("GiveJobUIMenu",job.id,"Has Been Given",true); notify("Tag","âœ… "..job.name.." aplicada!",2) end
                end)
            end)
        end
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 6: MUSIC ALL (Todas as MÃºsicas)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhMusicAllTab = newBhTab("Music All", "ðŸŽµ")
    do
        local RE = ReplicatedStorage:FindFirstChild("RE")
        local bhMusicRemote = RE and RE:FindFirstChild("PLAYEvent")

        -- Campo de pesquisa
        local sSearch=bhSection(bhMusicAllTab.page,"ðŸ” Pesquisar MÃºsica")
        local searchBox=bhTextbox(sSearch,"Pesquisar por nome ou ID...",2)

        local infoS=bhSection(bhMusicAllTab.page,"â„¹ï¸ Como Tocar")
        local iLbl=lbl(infoS,"ðŸ†• 4 IDs do YouTube + 75+ mÃºsicas verificadas!\nðŸ“‹ Clique em COPIAR para copiar o ID\nâ–¶ï¸ Clique em TOCAR para tocar direto",UDim2.new(1,0,0,50),nil,9,C.DIM)
        iLbl.TextWrapped=true; iLbl.LayoutOrder=2

        local allMusics = {
            -- ðŸ†• NOVOS 2025/2026
            {name="ðŸ†• Perigosa (Repost)",           id="993169998084000"},
            {name="ðŸ†• Assombra Matrix 9",           id="130149486547895"},
            {name="ðŸ†• Cocota Nesgreco",             id="130758596227702"},
            {name="ðŸ†• Mandela SP",                  id="137970023957031"},
            {name="Casal Mandrake",                  id="4890709034"},
            {name="Som do ParedÃ£o",                  id="6490661033"},
            {name="MC Don Juan - Confia Amiga",      id="2700449900"},
            {name="MC Zaquin - Ã” MoÃ§a",              id="6333384941"},
            {name="MC Menor ZL - Bandida",           id="6309420461"},
            {name="MC Xenon - MÃ³ TempÃ£o",            id="5991269354"},
            -- ðŸ”¥ FUNK BR
            {name="Baile de Favela",                 id="7254566675"},
            {name="Olha a ExplosÃ£o - MC Kevinho",    id="1132154508"},
            {name="Envolvimento - MC Loma",          id="6117112610"},
            {name="Toma Vapo Vapo - ZÃ© Felipe",      id="8175801924"},
            {name="SentadÃ£o - Pedro Sampaio",        id="4572496137"},
            {name="Amor ou o LitrÃ£o",                id="6117357206"},
            {name="MC Topre - Apaga Luz",            id="6318830246"},
            {name="Esqueci Como Namora - MC Leozin", id="4227656576"},
            {name="Golpista - MC Frog",              id="6254649803"},
            {name="Oh Juliana - MC Niack",           id="5794385002"},
            {name="To Voando Alto - MC Poze",        id="4432916296"},
            {name="MC Luan - Redes Sociais",         id="6549522717"},
            {name="MC Luan - Outras Vidas",          id="6520493218"},
            {name="Ja Chorei Demais - MC Lipi",      id="6740898532"},
            {name="Chorar Pra Que - MC Charada",     id="2699365781"},
            {name="Marilia Mendonca Funk",           id="4508655819"},
            {name="MC 3L - Um Sabadao Desse",        id="6052891776"},
            -- ðŸŽµ MONTAGEM
            {name="Montagem Mysterious Game",        id="1847850505"},
            {name="Automotivo Bibi Fogosa",          id="9046862941"},
            {name="MTG Quem Nao Quer",               id="9042781417"},
            {name="Visao de Futuro",                 id="6043914200"},
            {name="Chama No Zoom Close",             id="7714150300"},
            {name="Baile da Escocia",                id="5831100035"},
            -- ðŸ“± PHONK
            {name="Murder in My Mind",               id="6891418142"},
            {name="Cowboys Phonk",                   id="1845756489"},
            {name="Brazilian Phonk",                 id="8663653977"},
            {name="Beat Triste - DJ Sasori",         id="7123596246"},
            -- ðŸŽ¤ TRAP BR
            {name="GATA ONLY",                       id="6903302049"},
            {name="Malvadao 3",                      id="7487607735"},
            {name="Nego Drama - Racionais",          id="7819563881"},
            {name="Plutao - VMZ",                    id="6736643454"},
            {name="Maquina do Tempo - Matue",        id="7071495474"},
            -- ðŸ¤  PISEIRO
            {name="Baraoes da Pisadinha",            id="121187736532042"},
            {name="Toca o Trompete",                 id="6918766819"},
            {name="Recairei",                        id="6535162330"},
            -- ðŸ˜‚ MEMES
            {name="Funk Sub-Zero MK",                id="4282646399"},
            {name="Funk Bella Ciao",                 id="1583528384"},
            {name="Ameno Funk (Dorime)",             id="888328110"},
            {name="Funk Naruto Triste",              id="1472963352"},
            {name="Xuxa - Soco Bate Bira Funk",      id="2284321900"},
            -- ðŸŽ§ ELETRÃ”NICA
            {name="Faded - Alan Walker",             id="572129801"},
            {name="Alone - Marshmello",              id="887600872"},
            {name="Animals - Martin Garrix",         id="155693589"},
            {name="Titanium",                        id="155762585"},
        }

        -- Lista de IDs visÃ­veis (filtrada)
        local filteredMusics = allMusics
        local musicFrames = {}

        local s=bhSection(bhMusicAllTab.page,"ðŸŽµ "..(#allMusics).." MÃºsicas Verificadas")
        for i, music in ipairs(allMusics) do
            local mFrame=Instance.new("Frame",s)
            mFrame.Size=UDim2.new(1,0,0,32); mFrame.BackgroundColor3=Color3.fromRGB(18,8,32)
            mFrame.LayoutOrder=i+1; corner(mFrame,7); stroke(mFrame,Color3.fromRGB(60,20,100),1)
            mFrame.Name="MusicFrame_"..i

            lbl(mFrame,"ðŸŽµ "..music.name,UDim2.new(0.55,-4,1,0),UDim2.new(0,5,0,0),8,Color3.fromRGB(180,140,255),Enum.TextXAlignment.Left)

            -- BotÃ£o COPIAR
            local cpBtn=Instance.new("TextButton",mFrame)
            cpBtn.Size=UDim2.new(0.22,-2,0,24); cpBtn.Position=UDim2.new(0.55,2,0.5,-12)
            cpBtn.BackgroundColor3=Color3.fromRGB(20,60,100); cpBtn.Text="ðŸ“‹ Copiar"; cpBtn.TextSize=8
            cpBtn.Font=Enum.Font.GothamBold; cpBtn.TextColor3=C.WHITE; cpBtn.AutoButtonColor=false; cpBtn.ZIndex=52
            corner(cpBtn,5)
            cpBtn.MouseButton1Click:Connect(function()
                setclipboard(music.id); notify("Copiado!","ðŸ“‹ "..music.name,2)
                tween(cpBtn,{BackgroundColor3=C.GREEN},0.2); task.wait(0.5); tween(cpBtn,{BackgroundColor3=Color3.fromRGB(20,60,100)},0.3)
            end)

            -- BotÃ£o TOCAR
            local plBtn=Instance.new("TextButton",mFrame)
            plBtn.Size=UDim2.new(0.22,-2,0,24); plBtn.Position=UDim2.new(0.77,2,0.5,-12)
            plBtn.BackgroundColor3=Color3.fromRGB(18,60,22); plBtn.Text="â–¶ï¸ Tocar"; plBtn.TextSize=8
            plBtn.Font=Enum.Font.GothamBold; plBtn.TextColor3=C.WHITE; plBtn.AutoButtonColor=false; plBtn.ZIndex=52
            corner(plBtn,5)
            plBtn.MouseButton1Click:Connect(function()
                if bhMusicRemote then bhMusicRemote:FireServer(music.id)
                else setclipboard(music.id) end
                notify("â–¶ï¸ Tocando","ðŸŽµ "..music.name,2)
                tween(plBtn,{BackgroundColor3=C.GREEN},0.2); task.wait(0.5); tween(plBtn,{BackgroundColor3=Color3.fromRGB(18,60,22)},0.3)
            end)

            musicFrames[i]=mFrame
        end

        -- Pesquisa funcional
        searchBox.Changed:Connect(function(prop)
            if prop ~= "Text" then return end
            local q=string.lower(searchBox.Text)
            for i,music in ipairs(allMusics) do
                local frame=musicFrames[i]
                if frame then
                    local show = q=="" or string.find(string.lower(music.name),q,1,true) or string.find(music.id,q,1,true)
                    frame.Visible = show ~= nil
                end
            end
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 7: TROLL
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhTrollTab = newBhTab("Troll", "ðŸ˜ˆ")
    do
        local s=bhSection(bhTrollTab.page,"ðŸŽ¯ Alvo do Troll")
        local trollBox=bhTextbox(s,"Nome do jogador...",2)
        local listFT=Instance.new("Frame",s); listFT.Size=UDim2.new(1,0,0,0); listFT.AutomaticSize=Enum.AutomaticSize.Y; listFT.BackgroundTransparency=1; listFT.LayoutOrder=3
        local listLT=Instance.new("UIListLayout",listFT); listLT.Padding=UDim.new(0,2)
        local function refreshTrollList()
            for _,v in pairs(listFT:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            for _,p in pairs(Players:GetPlayers()) do
                local pb=Instance.new("TextButton",listFT); pb.Size=UDim2.new(1,0,0,24); pb.BackgroundColor3=Color3.fromRGB(20,10,35)
                pb.Text="ðŸ˜ˆ "..p.Name; pb.TextSize=9; pb.Font=Enum.Font.GothamBold; pb.TextColor3=Color3.fromRGB(255,100,100)
                pb.TextXAlignment=Enum.TextXAlignment.Left; pb.AutoButtonColor=false; pb.ZIndex=53; corner(pb,5)
                local padL=Instance.new("UIPadding",pb); padL.PaddingLeft=UDim.new(0,8)
                pb.MouseButton1Click:Connect(function() trollBox.Text=p.Name; bhTarget=p.Name; notify("Troll","Alvo: "..p.Name,1.5) end)
            end
        end
        bhBtn2(s,"ðŸ”„ Atualizar",Color3.fromRGB(30,15,55),4,refreshTrollList)
        refreshTrollList()

        local s2=bhSection(bhTrollTab.page,"ðŸ˜ˆ AÃ§Ãµes de Troll")
        local function getTrollTarget()
            local n=trollBox.Text ~= "" and trollBox.Text or bhTarget
            if not n then notify("Erro","Selecione o alvo!",2); return nil end
            local t=Players:FindFirstChild(n)
            if not t or not t.Character then notify("Erro","Jogador nÃ£o encontrado!",2); return nil end
            return t
        end

        bhBtn2(s2,"ðŸ’€ Matar",Color3.fromRGB(120,0,0),2,function()
            local t=getTrollTarget(); if not t then return end
            local hum=t.Character:FindFirstChildOfClass("Humanoid"); if hum then hum.Health=0; notify("Troll","â˜ ï¸ Morreu!",2) end
        end)
        bhBtn2(s2,"â¬†ï¸ Jogar p/ Cima (Fling)",Color3.fromRGB(60,30,0),3,function()
            local t=getTrollTarget(); if not t then return end
            local hrp=t.Character:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.AssemblyLinearVelocity=Vector3.new(math.random(-100,100),400,math.random(-100,100)); notify("Troll","â¬†ï¸ Voou!",2) end
        end)
        bhBtn2(s2,"ðŸŒ€ Girar (Fling AleatÃ³rio)",Color3.fromRGB(80,0,120),4,function()
            local t=getTrollTarget(); if not t then return end
            local hrp=t.Character:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.AssemblyLinearVelocity=Vector3.new(math.random(-400,400),300,math.random(-400,400)); notify("Troll","ðŸŒ€ Girou!",2) end
        end)
        bhBtn2(s2,"ðŸ“ Gigante",Color3.fromRGB(50,25,0),5,function()
            local t=getTrollTarget(); if not t then return end
            local hum=t.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.BodyHeightScale.Value=3; hum.BodyWidthScale.Value=3; hum.BodyDepthScale.Value=3; hum.HeadScale.Value=3; notify("Troll","ðŸ“ Ficou gigante!",2) end
        end)
        bhBtn2(s2,"ðŸ£ MinÃºsculo",Color3.fromRGB(25,50,0),6,function()
            local t=getTrollTarget(); if not t then return end
            local hum=t.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.BodyHeightScale.Value=0.1; hum.BodyWidthScale.Value=0.1; hum.BodyDepthScale.Value=0.1; hum.HeadScale.Value=0.1; notify("Troll","ðŸ£ Ficou minÃºsculo!",2) end
        end)
        bhBtn2(s2,"ðŸ’¬ Spam Mensagem",Color3.fromRGB(30,30,80),7,function()
            local t=getTrollTarget(); if not t then return end
            notify("Spam","ðŸ’¬ Spammando "..t.Name.."...",2)
            task.spawn(function()
                for i=1,10 do
                    pcall(function()
                        local re=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1RPNam1eTex1t")
                        if re then re:FireServer("RolePlayBio","AvenHub te trollou! ðŸ˜ˆ "..i) end
                    end)
                    task.wait(0.3)
                end
            end)
        end)
        bhBtn2(s2,"ðŸª‘ ForÃ§ar Sentar",Color3.fromRGB(30,55,18),8,function()
            local t=getTrollTarget(); if not t then return end
            local hum=t.Character:FindFirstChildOfClass("Humanoid"); if hum then hum.Sit=true; notify("Troll","ðŸª‘ Sentou!",2) end
        end)
        bhBtn2(s2,"ðŸ’¥ Cratera Nele",Color3.fromRGB(120,40,0),9,function()
            local t=getTrollTarget(); if not t then return end
            local hrp=t.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local exp=Instance.new("Explosion"); exp.Position=hrp.Position
                exp.BlastRadius=20; exp.BlastPressure=1000000; exp.DestroyJointRadiusPercent=0; exp.Parent=workspace
                notify("Troll","ðŸ’¥ Cratera!",2)
            end
        end)
        bhBtn2(s2,"ðŸ”Š Spam Sons",Color3.fromRGB(40,20,80),10,function()
            local t=getTrollTarget(); if not t then return end
            task.spawn(function()
                for i=1,5 do
                    pcall(function()
                        local remote=ReplicatedStorage:FindFirstChild("RemoteEvents") and ReplicatedStorage.RemoteEvents:FindFirstChild("PlayersHouse")
                        if remote then remote:FireServer("sound","1234567890") end
                    end)
                    task.wait(0.1)
                end
            end)
            notify("Troll","ðŸ”Š Spam de som!",2)
        end)

        local s3=bhSection(bhTrollTab.page,"ðŸ‘¥ Troll em TODOS")
        bhBtn2(s3,"ðŸ’€ Matar TODOS",Color3.fromRGB(120,0,0),2,function()
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local h=p.Character:FindFirstChildOfClass("Humanoid"); if h then h.Health=0 end
                end
            end; notify("Troll","â˜ ï¸ Todos morreram!",3)
        end)
        bhBtn2(s3,"ðŸŒ€ Fling TODOS",Color3.fromRGB(80,0,120),3,function()
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hrp=p.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then hrp.AssemblyLinearVelocity=Vector3.new(math.random(-400,400),300,math.random(-400,400)) end
                end
            end; notify("Troll","ðŸŒ€ Todos giraram!",3)
        end)
        bhBtn2(s3,"ðŸ“ Gigante TODOS",Color3.fromRGB(50,25,0),4,function()
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hum=p.Character:FindFirstChildOfClass("Humanoid")
                    if hum then hum.BodyHeightScale.Value=3; hum.BodyWidthScale.Value=3; hum.BodyDepthScale.Value=3; hum.HeadScale.Value=3 end
                end
            end; notify("Troll","ðŸ“ Todos ficaram gigantes!",3)
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 8: SPAM (AÃ§Ãµes de Spam)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhSpamTab = newBhTab("Spam", "ðŸ“¢")
    do
        local spamActive={}
        local s=bhSection(bhSpamTab.page,"ðŸ“¢ Spam de AÃ§Ãµes")

        local spamChatActive=false
        local spamChatBtn=bhBtn2(s,"ðŸ’¬ Spam Chat: OFF",Color3.fromRGB(30,15,55),2)
        spamChatBtn.MouseButton1Click:Connect(function()
            spamChatActive=not spamChatActive; spamChatBtn.Text=spamChatActive and "ðŸ’¬ Spam Chat: ON âœ…" or "ðŸ’¬ Spam Chat: OFF"
            tween(spamChatBtn,{BackgroundColor3=spamChatActive and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
            if spamChatActive then
                task.spawn(function()
                    while spamChatActive do
                        pcall(function()
                            game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents") and
                            game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents:FindFirstChild("SayMessageRequest") and
                            game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer("AvenHub V20 ðŸ”¥","All")
                        end)
                        task.wait(0.5)
                    end
                end)
            end
        end)

        local spamJumpActive=false
        local spamJumpBtn=bhBtn2(s,"â¬†ï¸ Spam Jump: OFF",Color3.fromRGB(30,15,55),3)
        spamJumpBtn.MouseButton1Click:Connect(function()
            spamJumpActive=not spamJumpActive; spamJumpBtn.Text=spamJumpActive and "â¬†ï¸ Spam Jump: ON âœ…" or "â¬†ï¸ Spam Jump: OFF"
            tween(spamJumpBtn,{BackgroundColor3=spamJumpActive and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
            if spamJumpActive then
                task.spawn(function()
                    while spamJumpActive do
                        local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid")
                        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
                        task.wait(0.1)
                    end
                end)
            end
        end)

        local spamCarActive=false
        local spamCarBtn=bhBtn2(s,"ðŸš— Spam Car: OFF",Color3.fromRGB(30,15,55),4)
        spamCarBtn.MouseButton1Click:Connect(function()
            spamCarActive=not spamCarActive; spamCarBtn.Text=spamCarActive and "ðŸš— Spam Car: ON âœ…" or "ðŸš— Spam Car: OFF"
            tween(spamCarBtn,{BackgroundColor3=spamCarActive and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
            if spamCarActive then
                task.spawn(function()
                    while spamCarActive do
                        pcall(function()
                            local re=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1Ca1r")
                            if re then
                                re:FireServer("PickingCar","RV")
                                re:FireServer("PickingCar","SchoolBus")
                                re:FireServer("PickingCar","Bus")
                            end
                        end)
                        task.wait(0.05)
                    end
                end)
            end
        end)

        local spamHeliActive=false
        local spamHeliBtn=bhBtn2(s,"ðŸš Spam HelicÃ³ptero: OFF",Color3.fromRGB(30,15,55),5)
        spamHeliBtn.MouseButton1Click:Connect(function()
            spamHeliActive=not spamHeliActive; spamHeliBtn.Text=spamHeliActive and "ðŸš Spam Heli: ON âœ…" or "ðŸš Spam HelicÃ³ptero: OFF"
            tween(spamHeliBtn,{BackgroundColor3=spamHeliActive and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
            if spamHeliActive then
                task.spawn(function()
                    while spamHeliActive do
                        pcall(function()
                            local re=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1Ca1r")
                            if re then re:FireServer("PickingCar","Helicopter") end
                        end)
                        task.wait(0.05)
                    end
                end)
            end
        end)

        bhBtn2(s,"ðŸ›‘ Parar TODOS os Spams",Color3.fromRGB(100,20,20),6,function()
            spamChatActive=false; spamJumpActive=false; spamCarActive=false; spamHeliActive=false
            spamChatBtn.Text="ðŸ’¬ Spam Chat: OFF"; tween(spamChatBtn,{BackgroundColor3=Color3.fromRGB(30,15,55)},0.15)
            spamJumpBtn.Text="â¬†ï¸ Spam Jump: OFF"; tween(spamJumpBtn,{BackgroundColor3=Color3.fromRGB(30,15,55)},0.15)
            spamCarBtn.Text="ðŸš— Spam Car: OFF"; tween(spamCarBtn,{BackgroundColor3=Color3.fromRGB(30,15,55)},0.15)
            spamHeliBtn.Text="ðŸš Spam HelicÃ³ptero: OFF"; tween(spamHeliBtn,{BackgroundColor3=Color3.fromRGB(30,15,55)},0.15)
            notify("Spam","ðŸ›‘ Todos os spams parados!",2)
        end)

        local s2=bhSection(bhSpamTab.page,"ðŸ’¬ Spam Mensagem Custom")
        local spamMsgBox=bhTextbox(s2,"Mensagem para spammar...",2)
        local spamMsgActive=false
        local spamMsgBtn=bhBtn2(s2,"ðŸ’¬ Spam Msg Custom: OFF",Color3.fromRGB(30,15,55),3)
        spamMsgBtn.MouseButton1Click:Connect(function()
            spamMsgActive=not spamMsgActive; spamMsgBtn.Text=spamMsgActive and "ðŸ’¬ Spam Msg: ON âœ…" or "ðŸ’¬ Spam Msg Custom: OFF"
            tween(spamMsgBtn,{BackgroundColor3=spamMsgActive and Color3.fromRGB(18,80,25) or Color3.fromRGB(30,15,55)},0.15)
            if spamMsgActive then
                task.spawn(function()
                    while spamMsgActive and spamMsgBox.Text~="" do
                        pcall(function()
                            local re=ReplicatedStorage:FindFirstChild("RE") and ReplicatedStorage.RE:FindFirstChild("1RPNam1eTex1t")
                            if re then re:FireServer("RolePlayBio",spamMsgBox.Text) end
                        end)
                        task.wait(0.3)
                    end
                end)
            end
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 9: SCRIPTS
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhScriptsTab = newBhTab("Scripts", "ðŸ“œ")
    do
        local s=bhSection(bhScriptsTab.page,"âš¡ Scripts Ãšteis")
        bhBtn2(s,"ðŸ”„ Rejoin Servidor",Color3.fromRGB(18,55,22),2,function()
            game:GetService("TeleportService"):Teleport(game.PlaceId,player)
        end)
        bhBtn2(s,"ðŸ“‹ Copiar Server ID",Color3.fromRGB(30,15,55),3,function()
            setclipboard(game.JobId); notify("Copiado!","ðŸ”— Server ID copiado!",2)
        end)
        bhBtn2(s,"ðŸ“‹ Copiar Place ID",Color3.fromRGB(30,15,55),4,function()
            setclipboard(tostring(game.PlaceId)); notify("Copiado!","ðŸ“‹ PlaceID: "..game.PlaceId,2)
        end)
        bhBtn2(s,"ðŸ—ºï¸ Teleport p/ InÃ­cio",Color3.fromRGB(18,55,80),5,function()
            local char=player.Character; if char and char:FindFirstChild("HumanoidRootPart") then
                char.HumanoidRootPart.CFrame=CFrame.new(0,10,0); notify("TP","ðŸ—ºï¸ InÃ­cio!",2)
            end
        end)
        bhBtn2(s,"ðŸ•µï¸ Salvar PosiÃ§Ã£o",C.ORANGE,6,function()
            local char=player.Character; if char and char:FindFirstChild("HumanoidRootPart") then
                local pos=char.HumanoidRootPart.Position
                local code=string.format("KB03|%.2f|%.2f|%.2f",pos.X,pos.Y,pos.Z)
                setclipboard(code); notify("PosiÃ§Ã£o","ðŸ“ Copiado: "..string.format("%.0f,%.0f,%.0f",pos.X,pos.Y,pos.Z),3)
            end
        end)
        bhBtn2(s,"ðŸ‘ï¸ ESP Jogadores: Toggle",Color3.fromRGB(30,60,80),7,function()
            for _,p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hrp=p.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        local existing=hrp:FindFirstChild("AvenESP")
                        if existing then existing:Destroy()
                        else
                            local bb=Instance.new("BillboardGui",hrp); bb.Name="AvenESP"
                            bb.Size=UDim2.new(0,80,0,30); bb.StudsOffset=Vector3.new(0,3,0); bb.AlwaysOnTop=true
                            local lbl2=Instance.new("TextLabel",bb); lbl2.Size=UDim2.new(1,0,1,0); lbl2.BackgroundTransparency=1
                            lbl2.Text=p.Name; lbl2.TextScaled=true; lbl2.Font=Enum.Font.GothamBold
                            lbl2.TextColor3=Color3.fromRGB(255,50,50); lbl2.TextStrokeTransparency=0
                        end
                    end
                end
            end
            notify("ESP","ðŸ‘ï¸ ESP alternado!",2)
        end)
        bhBtn2(s,"ðŸ  Ir para Casa PrÃ³pria",Color3.fromRGB(18,55,22),8,function()
            pcall(function()
                local homes=workspace:FindFirstChild("Houses") or workspace:FindFirstChild("Lots")
                if homes then
                    for _,home in pairs(homes:GetChildren()) do
                        if tostring(home:GetAttribute("Owner"))==tostring(player.UserId) then
                            local hrp=home:FindFirstChild("HumanoidRootPart") or home.PrimaryPart
                            if hrp and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                                player.Character.HumanoidRootPart.CFrame=CFrame.new(hrp.Position+Vector3.new(0,5,0))
                                notify("Casa","ðŸ  Sua casa encontrada!",2); return
                            end
                        end
                    end
                end
                notify("Casa","ðŸ  Teleportando para posiÃ§Ã£o base...",2)
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame=CFrame.new(-50,5,-30)
                end
            end)
        end)
        bhBtn2(s,"ðŸŒ Trocar Server (Random)",Color3.fromRGB(50,25,80),9,function()
            notify("Server","ðŸŒ Trocando servidor...",3)
            task.spawn(function()
                pcall(function()
                    local httpService=game:GetService("HttpService")
                    local teleportService=game:GetService("TeleportService")
                    local servers=teleportService:GetPlayerPlaceInstanceAsync(player.UserId)
                    teleportService:Teleport(game.PlaceId,player)
                end)
            end)
        end)

        local s2=bhSection(bhScriptsTab.page,"ðŸŽ¯ Anti-Troll RÃ¡pido")
        bhBtn2(s2,"ðŸ›¡ï¸ Sala Segura (Ir p/ Cima)",Color3.fromRGB(18,55,22),2,function()
            local char=player.Character; if char and char:FindFirstChild("HumanoidRootPart") then
                char.HumanoidRootPart.CFrame=CFrame.new(0,500,0); notify("Seguro","ðŸ›¡ï¸ VocÃª estÃ¡ no ar!",2)
            end
        end)
        bhBtn2(s2,"ðŸ”’ Travar PosiÃ§Ã£o: Toggle",Color3.fromRGB(50,25,0),3,function()
            local char=player.Character; if not char then return end
            local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
            hrp.Anchored = not hrp.Anchored
            notify("Lock",hrp.Anchored and "ðŸ”’ PosiÃ§Ã£o travada!" or "ðŸ”“ PosiÃ§Ã£o livre!",2)
        end)
    end

    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    -- SUB-ABA 10: MUSIC CUSTOM (ID Manual)
    -- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
    local bhMusicTab2 = newBhTab("Music", "ðŸŽ¶")
    do
        local RE = ReplicatedStorage:FindFirstChild("RE")
        local bhMusicRemote2 = RE and RE:FindFirstChild("PLAYEvent")

        local s=bhSection(bhMusicTab2.page,"ðŸŽ¶ Boombox Manual")
        local mIdBox=bhTextbox(s,"ID da mÃºsica...",2)
        bhBtn2(s,"â–¶ï¸ Tocar",Color3.fromRGB(18,55,22),3,function()
            if mIdBox.Text~="" then
                if bhMusicRemote2 then bhMusicRemote2:FireServer(mIdBox.Text)
                else setclipboard(mIdBox.Text) end
                notify("â–¶ï¸ MÃºsica","Tocando: "..mIdBox.Text,2)
            end
        end)
        bhBtn2(s,"â¹ Parar",Color3.fromRGB(80,20,20),4,function()
            if bhMusicRemote2 then bhMusicRemote2:FireServer("0"); notify("MÃºsica","â¹ Parado!",2) end
        end)
        bhBtn2(s,"ðŸ“‹ Copiar ID",Color3.fromRGB(18,55,80),5,function()
            if mIdBox.Text~="" then setclipboard(mIdBox.Text); notify("Copiado!","ðŸ“‹ "..mIdBox.Text,2) end
        end)
    end

    -- Ativar primeira aba
    task.defer(function()
        task.wait(0.05)
        if #bhTabs>0 then bhTabs[1].btn.MouseButton1Click:Fire() end
    end)

    -- Abrir painel ao clicar no botÃ£o Brookhaven+
    bhBtn.MouseButton1Click:Connect(function()
        bhPanel.Visible=true; bhPanel.Size=UDim2.new(0,0,0,0); bhPanel.Position=UDim2.new(0.5,0,0.5,0)
        tween(bhPanel,{Size=UDim2.new(0,BH_W,0,BH_H),Position=UDim2.new(0.5,-BH_W/2,0.5,-BH_H/2)},0.3,Enum.EasingStyle.Back,Enum.EasingDirection.Out)
        bhPanelOpen=true; tween(bhMiniBtn,{BackgroundColor3=Color3.fromRGB(20,80,25)},0.15)
        task.wait(0.05)
        if #bhTabs>0 then bhTabs[1].btn.MouseButton1Click:Fire() end
    end)
end

-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
-- ABA ADMIN
-- â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
local adminTab=newTab("Admin","âš™ï¸")
do
    local s_warn = section(adminTab.page, "âš ï¸ ADM PANEL")
    local warnCard = Instance.new("Frame", s_warn)
    warnCard.Size = UDim2.new(1,0,0,80); warnCard.BackgroundColor3 = Color3.fromRGB(40,10,10)
    warnCard.LayoutOrder = 2; corner(warnCard,10); stroke(warnCard,Color3.fromRGB(255,50,50),2)
    grad(warnCard,Color3.fromRGB(60,20,20),Color3.fromRGB(40,10,10),90)
    lbl(warnCard,"âš™ï¸ ADM PANEL",UDim2.new(1,0,0,28),UDim2.new(0,0,0,8),16,Color3.fromRGB(255,100,100))
    lbl(warnCard,"NÃƒO ESTÃ PRONTO",UDim2.new(1,0,0,22),UDim2.new(0,0,0,36),13,Color3.fromRGB(255,150,150))
    lbl(warnCard,"ðŸ• VAI TER EM BREVE - AGUARDE",UDim2.new(1,0,0,18),UDim2.new(0,0,0,58),10,Color3.fromRGB(255,200,200))

    local s_kb3 = section(adminTab.page, "ðŸ’€ Kill Bus 03 - Sistema de CÃ³digo")
    local infoKB3 = lbl(s_kb3, "âœ… MAS TEM ESSAS FUNÃ‡Ã•ES AQUI:\nâ€¢ Kill Bus 03 com sistema de cÃ³digo\nâ€¢ Gere cÃ³digo de qualquer lugar\nâ€¢ Leva jogador para o local do cÃ³digo",
        UDim2.new(1,0,0,65), nil, 11, C.GREEN)
    infoKB3.TextWrapped = true; infoKB3.LayoutOrder = 2

    local kb3PlayerBox = textbox(s_kb3, "Digite nome do jogador para levar...", 3)
    local listFrame3 = Instance.new("Frame", s_kb3)
    listFrame3.Size = UDim2.new(1,0,0,0); listFrame3.AutomaticSize = Enum.AutomaticSize.Y
    listFrame3.BackgroundTransparency = 1; listFrame3.LayoutOrder = 4
    local listLayout3 = Instance.new("UIListLayout", listFrame3); listLayout3.Padding = UDim.new(0,3)
    local function refreshKB3List()
        for _, v in pairs(listFrame3:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player then
                local pBtn = btn(listFrame3,"ðŸŽ¯ "..p.Name,UDim2.new(1,0,0,28),nil,C.CARD,11)
                pBtn.TextXAlignment = Enum.TextXAlignment.Left
                pBtn.MouseButton1Click:Connect(function() kb3PlayerBox.Text = p.Name; notify("Selecionado",p.Name,1) end)
            end
        end
    end
    actionBtn(s_kb3, "ðŸ”„ Atualizar Lista", C.CARD, 5, refreshKB3List)
    refreshKB3List()

    local kb3CodeBox = textbox(s_kb3, "Cole o cÃ³digo de coordenadas aqui...", 6)
    kb3CodeBox.TextSize = 10; kb3CodeBox.Size = UDim2.new(1,0,0,42)

    actionBtn(s_kb3, "ðŸ“ Gerar CÃ³digo (PosiÃ§Ã£o Atual)", C.ORANGE, 7, function()
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local pos = char.HumanoidRootPart.Position
            local code = string.format("KB03|%.2f|%.2f|%.2f", pos.X, pos.Y, pos.Z)
            kb3CodeBox.Text = code; setclipboard(code)
            notify("CÃ³digo Gerado!", "CÃ³digo copiado! ðŸ“‹", 2)
        end
    end)

    actionBtn(s_kb3, "ðŸ’€ KILL BUS 03 - Executar", Color3.fromRGB(200,0,150), 8, function()
        local targetName = kb3PlayerBox.Text
        local code = kb3CodeBox.Text
        if targetName == "" then notify("Erro","Selecione um jogador!",2); return end
        if code == "" then notify("Erro","Cole ou gere um cÃ³digo!",2); return end
        local parts = string.split(code, "|")
        if #parts ~= 4 or parts[1] ~= "KB03" then notify("Erro","CÃ³digo invÃ¡lido!",3); return end
        local destX,destY,destZ = tonumber(parts[2]),tonumber(parts[3]),tonumber(parts[4])
        if not destX then notify("Erro","Coordenadas invÃ¡lidas!",2); return end
        notify("Kill Bus 03", "Iniciando sequÃªncia... ðŸšŒ", 3)
        task.spawn(function()
            local success, err = pcall(function()
                local Character = player.Character
                if not Character then return end
                local Humanoid = Character:FindFirstChildOfClass("Humanoid")
                local RootPart = Character:FindFirstChild("HumanoidRootPart")
                local Vehicles = Workspace:FindFirstChild("Vehicles")
                if not Humanoid or not RootPart or not Vehicles then return end
                local OldPos = RootPart.CFrame
                notify("Kill Bus 03","Fase 1: Ãrea segura...",2)
                RootPart.CFrame = CFrame.new(500,150,500); task.wait(0.7)
                notify("Kill Bus 03","Fase 2: Preparando Ã´nibus...",2)
                local PCar = Vehicles:FindFirstChild(player.Name.."Car")
                if not PCar then
                    RootPart.CFrame = CFrame.new(1118.81,75.998,-1138.61); task.wait(0.5)
                    local remote = ReplicatedStorage:FindFirstChild("RE")
                    if remote then remote = remote:FindFirstChild("1Ca1r")
                        if remote then remote:FireServer("PickingCar","SchoolBus"); task.wait(0.6) end
                    end
                    PCar = Vehicles:FindFirstChild(player.Name.."Car")
                end
                if not PCar then notify("Erro","NÃ£o conseguiu spawnar Ã´nibus!",3); RootPart.CFrame = OldPos; return end
                notify("Kill Bus 03","Fase 3: Entrando no Ã´nibus...",2)
                local Seat = PCar:FindFirstChild("Body")
                if Seat then Seat = Seat:FindFirstChild("VehicleSeat") end
                if Seat then
                    local sa = 0
                    repeat task.wait(); RootPart.CFrame = Seat.CFrame*CFrame.new(0,math.random(-1,1),0); sa=sa+1 until Humanoid.Sit or not Seat.Parent or sa>30
                end
                task.wait(0.5)
                notify("Kill Bus 03","Fase 4: Localizando "..targetName.."...",2)
                local TargetPlayer = Players:FindFirstChild(targetName)
                if not TargetPlayer then
                    notify("Erro","Jogador nÃ£o encontrado!",2); Humanoid.Sit=false; task.wait(0.2); RootPart.CFrame=OldPos; return
                end
                local TargetC = TargetPlayer.Character
                local TargetH = TargetC and TargetC:FindFirstChildOfClass("Humanoid")
                local TargetRP = TargetC and TargetC:FindFirstChild("HumanoidRootPart")
                if not TargetC or not TargetH or not TargetRP then
                    notify("Erro","Jogador sem personagem!",2); Humanoid.Sit=false; task.wait(0.2); RootPart.CFrame=OldPos; return
                end
                if not TargetH.Sit then
                    notify("Kill Bus 03","Fase 5: Capturando...",2)
                    local attempts,maxAttempts = 0,100
                    while not TargetH.Sit and attempts < maxAttempts and TargetH.Health > 0 do
                        attempts=attempts+1; task.wait()
                        if not PCar or not PCar.PrimaryPart then break end
                        local rX,rY,rZ = math.random(-360,360),math.random(-360,360),math.random(-360,360)
                        local capFn = function(tgt,off,ang) pcall(function() PCar:SetPrimaryPartCFrame(CFrame.new(tgt.Position)*off*ang) end) end
                        capFn(TargetRP,CFrame.new(0,1,0)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                        capFn(TargetRP,CFrame.new(0,-2,4)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.05,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                        capFn(TargetRP,CFrame.new(2,1.5,-1)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.08,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                        capFn(TargetRP,CFrame.new(-2,-1.5,2)+TargetH.MoveDirection*TargetRP.Velocity.Magnitude/1.08,CFrame.Angles(math.rad(rX),math.rad(rY),math.rad(rZ)))
                        if attempts%20==0 then notify("Kill Bus 03",string.format("Capturando... %d%%",math.floor((attempts/maxAttempts)*100)),1) end
                    end
                    if TargetH.Sit then
                        notify("Kill Bus 03","âœ… Capturado!",2); task.wait(0.5)
                        notify("Kill Bus 03","Fase 6: Levando para destino...",2)
                        local destPos = Vector3.new(destX,destY,destZ)
                        if PCar and PCar.PrimaryPart then
                            local midPoint = Vector3.new((RootPart.Position.X+destX)/2,math.max(RootPart.Position.Y,destY)+50,(RootPart.Position.Z+destZ)/2)
                            PCar:SetPrimaryPartCFrame(CFrame.new(midPoint)); task.wait(0.3)
                            PCar:SetPrimaryPartCFrame(CFrame.new(destPos))
                        end
                        task.wait(1); notify("Kill Bus 03","ðŸŽ¯ Chegou!",2)
                        notify("Kill Bus 03","Fase 7: Retornando...",2)
                        if PCar and PCar.PrimaryPart then PCar:SetPrimaryPartCFrame(CFrame.new(0,-470,0)) end
                        task.wait(0.3); Humanoid.Sit=false; task.wait(0.2); RootPart.CFrame=OldPos
                        notify("Kill Bus 03","âœ… COMPLETO! "..targetName.." foi levado! ðŸŽ¯",4)
                    else
                        notify("Kill Bus 03","âŒ NÃ£o conseguiu capturar",3)
                        Humanoid.Sit=false; task.wait(0.2); RootPart.CFrame=OldPos
                    end
                end
            end)
            if not success then notify("Erro Fatal","Kill Bus 03 falhou: "..tostring(err),4) end
        end)
    end)

    local s_quick = section(adminTab.page, "âš¡ CÃ³digos RÃ¡pidos")
    local quickCodes = {
        {name="Passagem Secreta", code="KB03|71.00|-84.00|67.00"},
        {name="Casa 1", code="KB03|-50.00|5.00|-30.00"},
        {name="Casa 2", code="KB03|-25.00|5.00|-30.00"},
        {name="Supermercado", code="KB03|-80.00|5.00|-70.00"},
        {name="Parque", code="KB03|80.00|5.00|70.00"},
    }
    for i, quick in ipairs(quickCodes) do
        local qBtn = btn(s_quick,"ðŸ“‹ "..quick.name,UDim2.new(1,0,0,32),nil,C.ACCENT1,11)
        qBtn.LayoutOrder = i+1; qBtn.TextXAlignment = Enum.TextXAlignment.Left
        local padLeft = Instance.new("UIPadding",qBtn); padLeft.PaddingLeft = UDim.new(0,10)
        qBtn.MouseButton1Click:Connect(function()
            kb3CodeBox.Text = quick.code; setclipboard(quick.code)
            notify("CÃ³digo RÃ¡pido",quick.name.." copiado!",2)
            tween(qBtn,{BackgroundColor3=C.GREEN},0.2); task.wait(0.5); tween(qBtn,{BackgroundColor3=C.ACCENT1},0.3)
        end)
    end
end

-- Ativar Home
print("AvenHub V21 - GUI criada, ativando abas...")
task.defer(function()
    task.wait(0.15)
    pcall(function() homeTab.btn.MouseButton1Click:Fire() end)
end)

notify("AvenL Hub V21", "Brookhaven+ carregado! ðŸ”¥", 3)
print("âœ… AvenL Hub V21 FINAL - Carregado!")
print("ðŸŽ® Aba GAMES adicionada!")
print("ðŸ‘‘ By AVEN & MARIO")

end)
if not ok then
    print("âŒ ERRO AvenHub V21: " .. tostring(err))
end
